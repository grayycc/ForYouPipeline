{"id": 0, "parent_id": null, "operation": "baseline", "hypothesis": "I will reproduce the FM baseline exactly by importing the `FM` class from `baseline.py` and writing my own training loop that matches the reference implementation: 5 categorical fields (user_id, video_id, author_id, tab, dur_bucket), embedding dim k=16, Adam lr=0.001, batch size 8192, pointwise BCE loss, early stopping on validation primary with patience 4. I expect to land within the noise floor of 0.6016 (i.e., between ~0.599 and ~0.604). This confirms the pipeline works end-to-end before any modifications.", "code": "#!/usr/bin/env python3\n\"\"\"\nIteration 0: Reproduce the FM baseline.\nTrains an FM over 5 categorical fields, evaluates on validation,\nwrites submissions for valid and test.\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\n\n# \u2500\u2500 CLI \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nparser = argparse.ArgumentParser()\nparser.add_argument('--data_dir', required=True)\nparser.add_argument('--out_dir', required=True)\nparser.add_argument('--seed', type=int, default=0)\nparser.add_argument('--train_split', default='train',\n                    choices=['train', 'train+valid'])\nargs = parser.parse_args()\n\nos.makedirs(args.out_dir, exist_ok=True)\n\n# \u2500\u2500 Imports \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfrom data import load, encode\nfrom baseline import FM\nfrom evaluate import evaluate\nimport submit\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n# \u2500\u2500 Data \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nsplits = load(args.data_dir)\n\nenc, dim = encode(splits)\nX_tr, y_tr, u_tr = enc['train']\nX_va, y_va, u_va = enc['valid']\nX_te, y_te, u_te = enc['test']\n\n# \u2500\u2500 Training (train mode) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nBATCH = 8192\nPATIENCE = 4\nMAX_EPOCHS = 50\n\ndef run_epoch(model, X, y, rng, batch_size=BATCH):\n    \"\"\"One full pass over data in random order, returns mean loss.\"\"\"\n    idx = rng.permutation(len(y))\n    losses = []\n    for start in range(0, len(y), batch_size):\n        b = idx[start:start + batch_size]\n        loss = model.step(X[b], y[b])\n        losses.append(loss)\n    return float(np.mean(losses))\n\nif args.train_split == 'train':\n    rng = np.random.default_rng(args.seed)\n    model = FM(dim, k=16, lr=0.001, l2=1e-6, seed=args.seed)\n\n    best_val = -1.0\n    best_weights = None\n    patience_left = PATIENCE\n    best_epoch = 0\n\n    for epoch in range(1, MAX_EPOCHS + 1):\n        tr_loss = run_epoch(model, X_tr, y_tr, rng)\n\n        val_scores = model.predict(X_va)\n        val_res = evaluate(u_va, y_va, val_scores)\n        val_primary = val_res['primary']\n\n        print(f\"Epoch {epoch:02d}  tr_loss={tr_loss:.4f}  \"\n              f\"val_primary={val_primary:.4f}\", flush=True)\n\n        if val_primary > best_val + 1e-6:\n            best_val = val_primary\n            # Save weights\n            best_weights = (\n                model.V.copy(),\n                model.W.copy(),\n                float(model.b)\n            )\n            patience_left = PATIENCE\n            best_epoch = epoch\n        else:\n            patience_left -= 1\n            if patience_left <= 0:\n                print(f\"Early stop at epoch {epoch}, best was {best_epoch}\",\n                      flush=True)\n                break\n\n    # Restore best weights\n    model.V[:] = best_weights[0]\n    model.W[:] = best_weights[1]\n    model.b = best_weights[2]\n\n    # \u2500\u2500 Evaluate \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    tr_scores = model.predict(X_tr)\n    tr_res = evaluate(u_tr, y_tr, tr_scores)\n\n    va_scores = model.predict(X_va)\n    va_res = evaluate(u_va, y_va, va_scores)\n\n    # Unbiased evaluation\n    rand_rows = load_random_valid(args.data_dir)\n    X_rand, y_rand, u_rand, _ = encode_like_train(splits['train'], rand_rows)\n    unbiased = unbiased_primary(\n        args.data_dir, splits['train'],\n        lambda rows: model.predict(X_rand)\n    )\n\n    print(f\"TRAIN_PRIMARY={tr_res['primary']:.4f}\")\n    print(f\"VAL_GAUC={va_res['GAUC']:.4f}\")\n    print(f\"VAL_NDCG5={va_res['nDCG@5']:.4f}\")\n    print(f\"VAL_PRIMARY={va_res['primary']:.4f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.4f}\")\n\n    # \u2500\u2500 Write submissions \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        splits['valid'], va_scores\n    )\n    te_scores = model.predict(X_te)\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_scores\n    )\n\nelse:\n    # train+valid mode: combine train and valid, train for best_epoch epochs\n    # Use a fixed epoch count (15 epochs is typically where baseline converges)\n    X_all = np.concatenate([X_tr, X_va], axis=0)\n    y_all = np.concatenate([y_tr, y_va], axis=0)\n\n    FIXED_EPOCHS = 15  # conservative fixed schedule\n\n    rng = np.random.default_rng(args.seed)\n    model = FM(dim, k=16, lr=0.001, l2=1e-6, seed=args.seed)\n\n    for epoch in range(1, FIXED_EPOCHS + 1):\n        tr_loss = run_epoch(model, X_all, y_all, rng)\n        print(f\"Epoch {epoch:02d}  tr_loss={tr_loss:.4f}\", flush=True)\n\n    te_scores = model.predict(X_te)\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_scores\n    )", "plan": "", "spec": null, "gate_result": "", "review_verdict": "", "review_reason": "", "train_primary": 0.6918, "val_gauc": 0.6671, "val_ndcg5": 0.5358, "val_primary": 0.6014666666666667, "unbiased_val_primary": 0.3648, "seeds_averaged": 3, "seed_scores": [0.6015, 0.6018, 0.6011], "seed_unbiased_scores": [0.3638, 0.3667, 0.3639], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "Epoch 01  tr_loss=0.6391  val_primary=0.5869\nEpoch 02  tr_loss=0.5479  val_primary=0.5956\nEpoch 03  tr_loss=0.5129  val_primary=0.5993\nEpoch 04  tr_loss=0.5004  val_primary=0.5994\nEpoch 05  tr_loss=0.4941  val_primary=0.6010\nEpoch 06  tr_loss=0.4897  val_primary=0.6006\nEpoch 07  tr_loss=0.4859  val_primary=0.6015\nEpoch 08  tr_loss=0.4821  val_primary=0.6012\nEpoch 09  tr_loss=0.4784  val_primary=0.6007\nEpoch 10  tr_loss=0.4744  val_primary=0.5996\nEpoch 11  tr_loss=0.4705  val_primary=0.5990\nEarly stop at epoch 11, best was 7\nTRAIN_PRIMARY=0.6918\nVAL_GAUC=0.6671\nVAL_NDCG5=0.5358\nVAL_PRIMARY=0.6015\nUNBIASED_PRIMARY=0.3638\n", "stderr_tail": "", "exec_time": 26.25611066701822, "submission_ok": true, "accepted": true, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 545, "tokens_out": 1797, "wall_seconds": 97.89658633404179}
{"id": 1, "parent_id": null, "operation": "eda", "hypothesis": "I want to understand the data distribution to guide feature engineering and modeling choices. Specifically: (1) how many users have training history vs. are cold-start in validation, (2) the distribution of label rates per user/video, (3) what interaction signals might be useful for time-based features, and (4) whether there are patterns in the raw log columns that could inform feature construction without leakage.", "code": "import argparse\nimport csv\nimport os\nimport sys\nimport numpy as np\nfrom collections import defaultdict\n\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('--data_dir', required=True)\n    parser.add_argument('--out_dir', required=True)\n    parser.add_argument('--seed', type=int, default=0)\n    parser.add_argument('--train_split', default='train')\n    args = parser.parse_args()\n\n    data_dir = args.data_dir\n\n    # Load train log\n    train_file = os.path.join(data_dir, 'log_standard_4_08_to_4_21_pure.csv')\n    valid_test_file = os.path.join(data_dir, 'log_standard_4_22_to_5_08_pure.csv')\n\n    # Parse train\n    train_rows = []\n    with open(train_file) as f:\n        reader = csv.DictReader(f)\n        for row in reader:\n            train_rows.append(row)\n\n    print(f\"TRAIN_ROWS={len(train_rows)}\")\n\n    # Basic label stats\n    labels = [int(r['long_view']) for r in train_rows]\n    print(f\"TRAIN_LABEL_RATE={np.mean(labels):.4f}\")\n\n    # User stats in train\n    user_labels = defaultdict(list)\n    user_videos = defaultdict(set)\n    for r in train_rows:\n        uid = r['user_id']\n        user_labels[uid].append(int(r['long_view']))\n        user_videos[uid].add(r['video_id'])\n\n    n_train_users = len(user_labels)\n    rows_per_user = [len(v) for v in user_labels.values()]\n    print(f\"TRAIN_USERS={n_train_users}\")\n    print(f\"ROWS_PER_USER_median={np.median(rows_per_user):.1f} p10={np.percentile(rows_per_user,10):.1f} p90={np.percentile(rows_per_user,90):.1f}\")\n\n    # Video stats in train\n    video_labels = defaultdict(list)\n    for r in train_rows:\n        video_labels[r['video_id']].append(int(r['long_view']))\n    n_train_videos = len(video_labels)\n    print(f\"TRAIN_VIDEOS={n_train_videos}\")\n\n    # Parse valid+test\n    valid_rows, test_rows = [], []\n    with open(valid_test_file) as f:\n        reader = csv.DictReader(f)\n        for row in reader:\n            d = int(row['date'])\n            if 20220422 <= d <= 20220428:\n                valid_rows.append(row)\n            elif d >= 20220429:\n                test_rows.append(row)\n\n    print(f\"VALID_ROWS={len(valid_rows)}\")\n    print(f\"TEST_ROWS={len(test_rows)}\")\n\n    # Valid user coverage\n    valid_users = set(r['user_id'] for r in valid_rows)\n    cold_start = sum(1 for u in valid_users if u not in user_labels)\n    print(f\"VALID_USERS={len(valid_users)}\")\n    print(f\"VALID_COLD_START_USERS={cold_start} ({100*cold_start/len(valid_users):.1f}%)\")\n\n    # Valid label stats\n    valid_labels = [int(r['long_view']) for r in valid_rows]\n    print(f\"VALID_LABEL_RATE={np.mean(valid_labels):.4f}\")\n\n    valid_user_labels = defaultdict(list)\n    for r in valid_rows:\n        valid_user_labels[r['user_id']].append(int(r['long_view']))\n    \n    rows_per_valid_user = [len(v) for v in valid_user_labels.values()]\n    all_pos = sum(1 for v in valid_user_labels.values() if all(x==1 for x in v))\n    all_neg = sum(1 for v in valid_user_labels.values() if all(x==0 for x in v))\n    print(f\"VALID_ROWS_PER_USER_median={np.median(rows_per_valid_user):.1f} p10={np.percentile(rows_per_valid_user,10):.1f} p90={np.percentile(rows_per_valid_user,90):.1f}\")\n    print(f\"VALID_ALL_POS_USERS={all_pos} ({100*all_pos/len(valid_users):.1f}%)\")\n    print(f\"VALID_ALL_NEG_USERS={all_neg} ({100*all_neg/len(valid_users):.1f}%)\")\n\n    # Duration stats\n    durations = [float(r['duration_ms']) for r in train_rows]\n    print(f\"DURATION_MS_median={np.median(durations):.0f} p10={np.percentile(durations,10):.0f} p90={np.percentile(durations,90):.0f} max={np.max(durations):.0f}\")\n\n    # play_time vs duration relationship\n    play_times = np.array([float(r['play_time_ms']) for r in train_rows])\n    dur_arr = np.array([float(r['duration_ms']) for r in train_rows])\n    ratio = play_times / np.maximum(dur_arr, 1)\n    lv = np.array(labels)\n    print(f\"LONG_VIEW_RATE_ratio_gt1={np.mean(lv[ratio>=1]):.4f} n={np.sum(ratio>=1)}\")\n    print(f\"LONG_VIEW_RATE_ratio_lt1={np.mean(lv[ratio<1]):.4f} n={np.sum(ratio<1)}\")\n\n    # hourmin distribution in train\n    hours = [int(r['hourmin']) // 100 for r in train_rows]\n    hour_label = defaultdict(list)\n    for h, l in zip(hours, labels):\n        hour_label[h].append(l)\n    \n    # Top hours by label rate\n    hour_rates = {h: np.mean(v) for h, v in hour_label.items() if len(v) > 1000}\n    sorted_hours = sorted(hour_rates.items(), key=lambda x: x[1], reverse=True)\n    print(f\"TOP_HOURS_BY_LONG_VIEW: {sorted_hours[:5]}\")\n    print(f\"BOT_HOURS_BY_LONG_VIEW: {sorted_hours[-5:]}\")\n\n    # Tab distribution\n    tab_label = defaultdict(list)\n    for r in train_rows:\n        tab_label[r['tab']].append(int(r['long_view']))\n    print(f\"TAB_STATS: {[(t, len(v), f'{np.mean(v):.3f}') for t,v in sorted(tab_label.items())]}\")\n\n    # Video popularity in train vs valid\n    train_video_set = set(video_labels.keys())\n    valid_videos = set(r['video_id'] for r in valid_rows)\n    cold_videos = valid_videos - train_video_set\n    print(f\"VALID_VIDEOS={len(valid_videos)} COLD={len(cold_videos)} ({100*len(cold_videos)/len(valid_videos):.1f}%)\")\n\n    # is_click stats\n    click_lv = defaultdict(list)\n    for r in train_rows:\n        click_lv[int(r['is_click'])].append(int(r['long_view']))\n    print(f\"CLICK_VS_LONGVIEW: {[(k, len(v), f'{np.mean(v):.3f}') for k,v in sorted(click_lv.items())]}\")\n\nif __name__ == '__main__':\n    main()", "plan": "", "spec": null, "gate_result": "", "review_verdict": "", "review_reason": "", "train_primary": null, "val_gauc": null, "val_ndcg5": null, "val_primary": null, "unbiased_val_primary": null, "seeds_averaged": 1, "seed_scores": [], "seed_unbiased_scores": [], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "TRAIN_ROWS=1141112\nTRAIN_LABEL_RATE=0.3366\nTRAIN_USERS=26210\nROWS_PER_USER_median=31.0 p10=5.0 p90=97.0\nTRAIN_VIDEOS=7538\nVALID_ROWS=124909\nTEST_ROWS=170588\nVALID_USERS=22377\nVALID_COLD_START_USERS=422 (1.9%)\nVALID_LABEL_RATE=0.3133\nVALID_ROWS_PER_USER_median=4.0 p10=1.0 p90=12.0\nVALID_ALL_POS_USERS=2663 (11.9%)\nVALID_ALL_NEG_USERS=6785 (30.3%)\nDURATION_MS_median=70233 p10=11633 p90=235766 max=1177720\nLONG_VIEW_RATE_ratio_gt1=0.9061 n=193757\nLONG_VIEW_RATE_ratio_lt1=0.2202 n=947355\nTOP_HOURS_BY_LONG_VIEW: [(7, np.float64(0.37636614112964945)), (8, np.float64(0.37453649616498197)), (6, np.float64(0.36801872074882996)), (13, np.float64(0.35263149447057124)), (12, np.float64(0.3502220943981082))]\nBOT_HOURS_BY_LONG_VIEW: [(15, np.float64(0.3269177622488057)), (21, np.float64(0.32631640193169253)), (20, np.float64(0.3251695701608793)), (17, np.float64(0.3232733127423277)), (16, np.float64(0.3178464749703099))]\nTAB_STATS: [('0', 150013, '0.042'), ('1', 834876, '0.386'), ('10', 80, '0.613'), ('11', 417, '0.115'), ('12', 834, '0.096'), ('13', 283, '0.180'), ('14', 11, '0.000'), ('2', 39291, '0.381'), ('3', 3574, '0.004'), ('4', 75524, '0.489'), ('5', 3402, '0.170'), ('6', 29671, '0.087'), ('7', 333, '0.213'), ('8', 2551, '0.018'), ('9', 252, '0.167')]\nVALID_VIDEOS=5951 COLD=7 (0.1%)\nCLICK_VS_LONGVIEW: [(0, 612267, '0.003'), (1, 528845, '0.723')]\n", "stderr_tail": "", "exec_time": 3.452610416978132, "submission_ok": false, "accepted": false, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 296, "tokens_out": 2126, "wall_seconds": 22.241689999995288}
{"id": 2, "parent_id": 0, "operation": "improve", "hypothesis": "Averaging within-user fractional ranks across 5 independently-seeded FM models will improve validation primary by ~0.0016 over a single-seed model, because rank averaging reduces variance in the ordering decisions that directly determine GAUC and nDCG@5", "code": "#!/usr/bin/env python3\n\"\"\"\nIteration 1: Seed ensemble with within-user rank averaging.\nTrain 5 FM models with seeds 0-4, convert each model's scores to within-user\nfractional ranks, average ranks across models, use averaged rank as final score.\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\n\n# \u2500\u2500 CLI \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nparser = argparse.ArgumentParser()\nparser.add_argument('--data_dir', required=True)\nparser.add_argument('--out_dir', required=True)\nparser.add_argument('--seed', type=int, default=0)\nparser.add_argument('--train_split', default='train',\n                    choices=['train', 'train+valid'])\nargs = parser.parse_args()\n\nos.makedirs(args.out_dir, exist_ok=True)\n\n# \u2500\u2500 Imports \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfrom data import load, encode\nfrom baseline import FM\nfrom evaluate import evaluate\nimport submit\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n# \u2500\u2500 Data \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nsplits = load(args.data_dir)\n\nenc, dim = encode(splits)\nX_tr, y_tr, u_tr = enc['train']\nX_va, y_va, u_va = enc['valid']\nX_te, y_te, u_te = enc['test']\n\n# \u2500\u2500 Hyperparameters \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nBATCH = 8192\nPATIENCE = 4\nMAX_EPOCHS = 50\nN_SEEDS = 5\n\n# \u2500\u2500 Helper: within-user fractional ranks \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef within_user_ranks(users, scores):\n    \"\"\"\n    For each user, convert raw scores to fractional ranks in [0, 1].\n    Rank 0 = lowest score, rank 1 = highest score (within that user's items).\n    Uses scipy-style fractional ranking to handle ties gracefully.\n    \"\"\"\n    user_arr = np.array(users)\n    ranks = np.zeros(len(scores), dtype=np.float64)\n    unique_users = np.unique(user_arr)\n    for u in unique_users:\n        mask = user_arr == u\n        s = scores[mask]\n        n = mask.sum()\n        if n == 1:\n            ranks[mask] = 0.5\n            continue\n        # argsort of argsort gives rank (0-based), then normalize to [0,1]\n        order = np.argsort(s)\n        r = np.empty(n, dtype=np.float64)\n        r[order] = np.arange(n, dtype=np.float64)\n        # fractional rank in [0, 1]\n        if n > 1:\n            r = r / (n - 1)\n        else:\n            r = np.full(n, 0.5)\n        ranks[mask] = r\n    return ranks\n\n# \u2500\u2500 Training helper \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef run_epoch(model, X, y, rng, batch_size=BATCH):\n    \"\"\"One full pass over data in random order, returns mean loss.\"\"\"\n    idx = rng.permutation(len(y))\n    losses = []\n    for start in range(0, len(y), batch_size):\n        b = idx[start:start + batch_size]\n        loss = model.step(X[b], y[b])\n        losses.append(loss)\n    return float(np.mean(losses))\n\n\ndef train_single_model(seed, X_tr, y_tr, X_va, y_va, u_va, dim):\n    \"\"\"Train one FM model with given seed, return best model weights and epoch count.\"\"\"\n    rng = np.random.default_rng(seed)\n    model = FM(dim, k=16, lr=0.001, l2=1e-6, seed=seed)\n\n    best_val = -1.0\n    best_weights = None\n    patience_left = PATIENCE\n    best_epoch = 0\n\n    for epoch in range(1, MAX_EPOCHS + 1):\n        tr_loss = run_epoch(model, X_tr, y_tr, rng)\n\n        val_scores = model.predict(X_va)\n        val_res = evaluate(u_va, y_va, val_scores)\n        val_primary = val_res['primary']\n\n        print(f\"  Seed {seed} Epoch {epoch:02d}  tr_loss={tr_loss:.4f}  \"\n              f\"val_primary={val_primary:.4f}\", flush=True)\n\n        if val_primary > best_val + 1e-6:\n            best_val = val_primary\n            best_weights = (\n                model.V.copy(),\n                model.W.copy(),\n                float(model.b)\n            )\n            patience_left = PATIENCE\n            best_epoch = epoch\n        else:\n            patience_left -= 1\n            if patience_left <= 0:\n                print(f\"  Seed {seed} early stop at epoch {epoch}, best was {best_epoch}\",\n                      flush=True)\n                break\n\n    # Restore best weights\n    model.V[:] = best_weights[0]\n    model.W[:] = best_weights[1]\n    model.b = best_weights[2]\n\n    return model, best_epoch\n\n\nif args.train_split == 'train':\n    # \u2500\u2500 Train N_SEEDS models and collect scores \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    va_rank_sum = np.zeros(len(y_va), dtype=np.float64)\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n    tr_rank_sum = np.zeros(len(y_tr), dtype=np.float64)\n    rand_rank_sum = None\n    u_rand_arr = None\n\n    # Load random data once for unbiased evaluation\n    rand_rows = load_random_valid(args.data_dir)\n    X_rand, y_rand, u_rand, _ = encode_like_train(splits['train'], rand_rows)\n    rand_rank_sum = np.zeros(len(y_rand), dtype=np.float64)\n\n    best_epochs = []\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} ===\", flush=True)\n        model, best_epoch = train_single_model(s, X_tr, y_tr, X_va, y_va, u_va, dim)\n        best_epochs.append(best_epoch)\n\n        # Compute scores for each split\n        va_scores = model.predict(X_va)\n        te_scores = model.predict(X_te)\n        tr_scores = model.predict(X_tr)\n        rand_scores = model.predict(X_rand)\n\n        # Convert to within-user fractional ranks\n        va_ranks = within_user_ranks(u_va, va_scores)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        tr_ranks = within_user_ranks(u_tr, tr_scores)\n        rand_ranks = within_user_ranks(u_rand, rand_scores)\n\n        va_rank_sum += va_ranks\n        te_rank_sum += te_ranks\n        tr_rank_sum += tr_ranks\n        rand_rank_sum += rand_ranks\n\n    # Average ranks\n    va_final = va_rank_sum / N_SEEDS\n    te_final = te_rank_sum / N_SEEDS\n    tr_final = tr_rank_sum / N_SEEDS\n    rand_final = rand_rank_sum / N_SEEDS\n\n    print(f\"\\nBest epochs across seeds: {best_epochs}\", flush=True)\n\n    # \u2500\u2500 Evaluate \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    tr_res = evaluate(u_tr, y_tr, tr_final)\n    va_res = evaluate(u_va, y_va, va_final)\n\n    # Unbiased evaluation using averaged ranks\n    unbiased = unbiased_primary(\n        args.data_dir, splits['train'],\n        lambda rows: rand_final\n    )\n\n    print(f\"TRAIN_PRIMARY={tr_res['primary']:.4f}\")\n    print(f\"VAL_GAUC={va_res['GAUC']:.4f}\")\n    print(f\"VAL_NDCG5={va_res['nDCG@5']:.4f}\")\n    print(f\"VAL_PRIMARY={va_res['primary']:.4f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.4f}\")\n\n    # \u2500\u2500 Write submissions \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        splits['valid'], va_final\n    )\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )\n\nelse:\n    # train+valid mode: combine train and valid, train for average best_epoch epochs\n    # Use fixed epoch count based on typical convergence\n    X_all = np.concatenate([X_tr, X_va], axis=0)\n    y_all = np.concatenate([y_tr, y_va], axis=0)\n    u_all = list(u_tr) + list(u_va)\n\n    FIXED_EPOCHS = 10  # conservative fixed schedule for train+valid\n\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} (train+valid) ===\", flush=True)\n        rng = np.random.default_rng(s)\n        model = FM(dim, k=16, lr=0.001, l2=1e-6, seed=s)\n\n        for epoch in range(1, FIXED_EPOCHS + 1):\n            tr_loss = run_epoch(model, X_all, y_all, rng)\n            print(f\"  Seed {s} Epoch {epoch:02d}  tr_loss={tr_loss:.4f}\", flush=True)\n\n        te_scores = model.predict(X_te)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        te_rank_sum += te_ranks\n\n    te_final = te_rank_sum / N_SEEDS\n\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )", "plan": "", "spec": {"hypothesis": "Averaging within-user fractional ranks across 5 independently-seeded FM models will improve validation primary by ~0.0016 over a single-seed model, because rank averaging reduces variance in the ordering decisions that directly determine GAUC and nDCG@5", "mechanism": "Each FM seed produces slightly different score values due to random initialization; some seeds rank a pair correctly, others incorrectly. Averaging fractional ranks (not raw scores) aggregates the ordering signal in rank space, which is exactly what GAUC and nDCG@5 measure. Rank-space averaging works better than score-space averaging because the metric is ordinal: a seed that is confidently wrong in score space but correct in rank space still contributes correctly after rank conversion.", "evidence": {"task_structure": "With only ~5 impressions per user, each pairwise comparison is high-variance. A single model's random initialization can systematically mis-rank a pair; ensemble majority reduces this. The within-user structure means rank averaging is directly optimizing the evaluation criterion.", "previous_experiments": "The established findings section explicitly documents: 'Seed ensembling with within-user rank averaging: +0.0016. Train 5 copies of the model on seeds 0-4, convert each model scores to within-user fractional ranks, then average the ranks rather than the raw scores. Averaging raw scores gave about half as much.' This was measured on the same split with the same FM model.", "literature": ""}, "target_metric": "both", "proposed_change": "Train 5 FM models with seeds 0-4, compute per-model within-user fractional ranks, average the ranks across models, use averaged rank as the final score for submission", "expected_result": "VAL_PRIMARY around 0.6031 (+0.0016 over 0.6015 baseline), potentially up to 0.6039 if the smoothed-CTR interaction helps", "falsification_condition": "If VAL_PRIMARY is below 0.6025 (within noise of baseline), the ensembling mechanism is not working as documented for this seed/configuration", "risks": {"leakage": "None \u2014 each model uses only train-split data with the standard encode() pipeline", "overfitting": "Low \u2014 ensembling reduces variance rather than fitting more parameters", "runtime": "5x single model training; FM converges in ~7 epochs so ~35 epochs total, should be well under 25 minutes"}, "implementation_scope": ["solution.py"], "tags": ["ensembling", "rank-averaging", "variance-reduction", "fm"], "candidates_considered": ["5-seed ensemble with within-user rank averaging: documented +0.0016, above noise floor if it hits best case, 50% acceptance rate historically", "Bayesian-smoothed per-video CTR bucketed feature: documented +0.0008, below noise floor alone, better as complement to ensembling", "LambdaRank/listwise ranking objective: 25 attempts, 4 accepts, median delta -0.0014 \u2014 poor historical record"], "reflection": "Node 0 reproduced the baseline at 0.6015 (within noise of 0.6016). Node 1 was EDA only. The only mechanism with consistent positive results across pooled history is ensembling (+0.0016 documented, 50% acceptance). Everything else either lands in noise or actively hurts. The established findings explicitly state that 5-seed ensemble with within-user rank averaging gave +0.0016 on this exact split, making it the highest-expected-value single change available.", "feature_provenance": null}, "gate_result": "passed_after_replan", "review_verdict": "CLEAN", "review_reason": "", "train_primary": 0.6863, "val_gauc": 0.6691, "val_ndcg5": 0.5366, "val_primary": 0.6028, "unbiased_val_primary": 0.3653, "seeds_averaged": 3, "seed_scores": [0.6028, 0.6028, 0.6028], "seed_unbiased_scores": [0.3653, 0.3653, 0.3653], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "poch 11  tr_loss=0.4704  val_primary=0.5974\n  Seed 2 early stop at epoch 11, best was 7\n\n=== Training seed 3 ===\n  Seed 3 Epoch 01  tr_loss=0.6404  val_primary=0.5884\n  Seed 3 Epoch 02  tr_loss=0.5520  val_primary=0.5947\n  Seed 3 Epoch 03  tr_loss=0.5149  val_primary=0.5986\n  Seed 3 Epoch 04  tr_loss=0.5014  val_primary=0.6000\n  Seed 3 Epoch 05  tr_loss=0.4951  val_primary=0.6011\n  Seed 3 Epoch 06  tr_loss=0.4906  val_primary=0.6015\n  Seed 3 Epoch 07  tr_loss=0.4868  val_primary=0.6009\n  Seed 3 Epoch 08  tr_loss=0.4834  val_primary=0.6001\n  Seed 3 Epoch 09  tr_loss=0.4798  val_primary=0.6000\n  Seed 3 Epoch 10  tr_loss=0.4761  val_primary=0.5994\n  Seed 3 early stop at epoch 10, best was 6\n\n=== Training seed 4 ===\n  Seed 4 Epoch 01  tr_loss=0.6404  val_primary=0.5884\n  Seed 4 Epoch 02  tr_loss=0.5506  val_primary=0.5960\n  Seed 4 Epoch 03  tr_loss=0.5132  val_primary=0.6001\n  Seed 4 Epoch 04  tr_loss=0.5001  val_primary=0.6009\n  Seed 4 Epoch 05  tr_loss=0.4937  val_primary=0.6018\n  Seed 4 Epoch 06  tr_loss=0.4894  val_primary=0.6016\n  Seed 4 Epoch 07  tr_loss=0.4855  val_primary=0.6020\n  Seed 4 Epoch 08  tr_loss=0.4817  val_primary=0.6015\n  Seed 4 Epoch 09  tr_loss=0.4777  val_primary=0.6004\n  Seed 4 Epoch 10  tr_loss=0.4737  val_primary=0.5992\n  Seed 4 Epoch 11  tr_loss=0.4697  val_primary=0.5986\n  Seed 4 early stop at epoch 11, best was 7\n\nBest epochs across seeds: [7, 4, 7, 6, 7]\nTRAIN_PRIMARY=0.6863\nVAL_GAUC=0.6691\nVAL_NDCG5=0.5366\nVAL_PRIMARY=0.6028\nUNBIASED_PRIMARY=0.3653\n", "stderr_tail": "", "exec_time": 334.61146825004835, "submission_ok": true, "accepted": true, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 19123, "tokens_out": 9589, "wall_seconds": 1157.6521445830003}
{"id": 3, "parent_id": 2, "operation": "improve", "hypothesis": "Adding Bayesian-smoothed per-video and per-author historical long_view rates (bucketed into categorical features) to the FM on top of the 5-seed rank ensemble will increase validation primary by ~+0.0008, pushing the combined system to ~0.6036+", "code": "#!/usr/bin/env python3\n\"\"\"\nIteration 2: Add Bayesian-smoothed per-video and per-author long_view rates (bucketed)\nto the 5-seed FM ensemble with within-user rank averaging.\n\nChanges from iteration 1:\n- Compute Bayesian-smoothed long_view rate for each video and author from training data only\n- Bucket each into 10 equal-frequency bins\n- Add these 2 new categorical features to FM input (7 features total instead of 5)\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\n\n# \u2500\u2500 CLI \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nparser = argparse.ArgumentParser()\nparser.add_argument('--data_dir', required=True)\nparser.add_argument('--out_dir', required=True)\nparser.add_argument('--seed', type=int, default=0)\nparser.add_argument('--train_split', default='train',\n                    choices=['train', 'train+valid'])\nargs = parser.parse_args()\n\nos.makedirs(args.out_dir, exist_ok=True)\n\n# \u2500\u2500 Imports \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfrom data import load, encode, FIELDS\nfrom baseline import FM\nfrom evaluate import evaluate\nimport submit\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n# \u2500\u2500 Data \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nsplits = load(args.data_dir)\n\nenc, dim = encode(splits)\nX_tr_base, y_tr, u_tr = enc['train']\nX_va_base, y_va, u_va = enc['valid']\nX_te_base, y_te, u_te = enc['test']\n\n# \u2500\u2500 Compute Bayesian-smoothed CTR features from training data only \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n# Fields: user_id=0, video_id=1, author_id=2, tab=3, dur_bucket=4, long_view=5 in raw rows\n# We use splits['train'] which has tuples: (date, user_id, video_id, author_id, tab, duration_ms, long_view)\n\nGLOBAL_MEAN_PRIOR_STRENGTH = 50  # pseudo-count for Bayesian smoothing\n\ndef compute_bayesian_ctr(train_rows, key_idx, label_idx=6):\n    \"\"\"\n    Compute Bayesian-smoothed long_view rate keyed by field at key_idx.\n    Returns a dict: key -> smoothed_rate\n    key_idx: index into the row tuple (1=user_id, 2=video_id, 3=author_id)\n    label_idx: index of long_view label (6 in load() tuples)\n    \"\"\"\n    counts = {}  # key -> [total, positives]\n    for row in train_rows:\n        key = row[key_idx]\n        label = row[label_idx]\n        if key not in counts:\n            counts[key] = [0, 0]\n        counts[key][0] += 1\n        counts[key][1] += label\n\n    # Global rate (prior)\n    total_all = sum(v[0] for v in counts.values())\n    pos_all = sum(v[1] for v in counts.values())\n    global_rate = pos_all / total_all if total_all > 0 else 0.5\n\n    # Bayesian smoothing: rate = (positives + alpha * global_rate) / (total + alpha)\n    alpha = GLOBAL_MEAN_PRIOR_STRENGTH\n    smoothed = {}\n    for key, (total, pos) in counts.items():\n        smoothed[key] = (pos + alpha * global_rate) / (total + alpha)\n\n    return smoothed, global_rate\n\n\ndef make_bucket_feature(rows, ctr_map, global_rate, key_idx, n_bins=10):\n    \"\"\"\n    For each row, look up the Bayesian-smoothed CTR and bucket it.\n    Returns integer array of bucket indices (0 to n_bins-1).\n    Unknown keys get global_rate.\n    \"\"\"\n    rates = np.array([\n        ctr_map.get(row[key_idx], global_rate)\n        for row in rows\n    ], dtype=np.float64)\n    return rates\n\n\ndef compute_buckets_from_rates(train_rates, query_rates, n_bins=10):\n    \"\"\"\n    Use quantile edges from training rates to bucket query rates.\n    Returns integer bucket indices (0 to n_bins-1).\n    \"\"\"\n    # Compute quantile edges from training rates\n    quantiles = np.linspace(0, 100, n_bins + 1)\n    edges = np.percentile(train_rates, quantiles)\n    # Ensure edges are unique; if not, use unique edges\n    edges = np.unique(edges)\n    # Bucket query rates using these edges\n    # np.searchsorted: values below first edge -> 0, above last -> len(edges)\n    buckets = np.searchsorted(edges[1:-1], query_rates, side='right')\n    # Clip to [0, n_bins-1]\n    buckets = np.clip(buckets, 0, n_bins - 1)\n    return buckets.astype(np.int32)\n\n\n# Compute CTR maps from training data only\ntrain_rows = splits['train']\nvalid_rows = splits['valid']\ntest_rows = splits['test']\n\nprint(\"Computing video CTR map...\", flush=True)\nvideo_ctr_map, video_global_rate = compute_bayesian_ctr(train_rows, key_idx=2)  # video_id at idx 2\nprint(f\"  {len(video_ctr_map)} videos, global rate={video_global_rate:.4f}\", flush=True)\n\nprint(\"Computing author CTR map...\", flush=True)\nauthor_ctr_map, author_global_rate = compute_bayesian_ctr(train_rows, key_idx=3)  # author_id at idx 3\nprint(f\"  {len(author_ctr_map)} authors, global rate={author_global_rate:.4f}\", flush=True)\n\n# Get rates for all splits\nN_BINS = 10\n\ntrain_video_rates = make_bucket_feature(train_rows, video_ctr_map, video_global_rate, key_idx=2)\ntrain_author_rates = make_bucket_feature(train_rows, author_ctr_map, author_global_rate, key_idx=3)\n\nvalid_video_rates = make_bucket_feature(valid_rows, video_ctr_map, video_global_rate, key_idx=2)\nvalid_author_rates = make_bucket_feature(valid_rows, author_ctr_map, author_global_rate, key_idx=3)\n\ntest_video_rates = make_bucket_feature(test_rows, video_ctr_map, video_global_rate, key_idx=2)\ntest_author_rates = make_bucket_feature(test_rows, author_ctr_map, author_global_rate, key_idx=3)\n\n# Compute bucket assignments using training quantiles\ntrain_video_buckets = compute_buckets_from_rates(train_video_rates, train_video_rates, N_BINS)\ntrain_author_buckets = compute_buckets_from_rates(train_author_rates, train_author_rates, N_BINS)\n\nvalid_video_buckets = compute_buckets_from_rates(train_video_rates, valid_video_rates, N_BINS)\nvalid_author_buckets = compute_buckets_from_rates(train_author_rates, valid_author_rates, N_BINS)\n\ntest_video_buckets = compute_buckets_from_rates(train_video_rates, test_video_rates, N_BINS)\ntest_author_buckets = compute_buckets_from_rates(train_author_rates, test_author_rates, N_BINS)\n\nprint(f\"Video CTR bucket distribution (train): {np.bincount(train_video_buckets)}\", flush=True)\nprint(f\"Author CTR bucket distribution (train): {np.bincount(train_author_buckets)}\", flush=True)\n\n# \u2500\u2500 Extend feature matrices with new CTR bucket features \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n# Add offset to new feature indices to avoid collision with existing features\n# Existing dim covers indices 0..dim-1\n# New features: video_ctr_bucket (0..N_BINS-1) and author_ctr_bucket (0..N_BINS-1)\n# They each get their own slot range after dim\n\nVIDEO_CTR_OFFSET = dim\nAUTHOR_CTR_OFFSET = dim + N_BINS\nNEW_DIM = dim + N_BINS + N_BINS\n\nprint(f\"Original embedding dim: {dim}, New dim: {NEW_DIM}\", flush=True)\n\ndef extend_features(X_base, video_buckets, author_buckets):\n    \"\"\"Append video_ctr and author_ctr bucket columns to feature matrix.\"\"\"\n    vid_col = (video_buckets + VIDEO_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    auth_col = (author_buckets + AUTHOR_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    return np.concatenate([X_base, vid_col, auth_col], axis=1)\n\nX_tr = extend_features(X_tr_base, train_video_buckets, train_author_buckets)\nX_va = extend_features(X_va_base, valid_video_buckets, valid_author_buckets)\nX_te = extend_features(X_te_base, test_video_buckets, test_author_buckets)\n\nprint(f\"Feature matrix shapes: train={X_tr.shape}, valid={X_va.shape}, test={X_te.shape}\", flush=True)\n\n# \u2500\u2500 Hyperparameters \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nBATCH = 8192\nPATIENCE = 4\nMAX_EPOCHS = 50\nN_SEEDS = 5\n\n# \u2500\u2500 Helper: within-user fractional ranks \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef within_user_ranks(users, scores):\n    \"\"\"\n    For each user, convert raw scores to fractional ranks in [0, 1].\n    Rank 0 = lowest score, rank 1 = highest score (within that user's items).\n    \"\"\"\n    user_arr = np.array(users)\n    ranks = np.zeros(len(scores), dtype=np.float64)\n    unique_users = np.unique(user_arr)\n    for u in unique_users:\n        mask = user_arr == u\n        s = scores[mask]\n        n = mask.sum()\n        if n == 1:\n            ranks[mask] = 0.5\n            continue\n        order = np.argsort(s)\n        r = np.empty(n, dtype=np.float64)\n        r[order] = np.arange(n, dtype=np.float64)\n        r = r / (n - 1)\n        ranks[mask] = r\n    return ranks\n\n# \u2500\u2500 Training helper \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef run_epoch(model, X, y, rng, batch_size=BATCH):\n    \"\"\"One full pass over data in random order, returns mean loss.\"\"\"\n    idx = rng.permutation(len(y))\n    losses = []\n    for start in range(0, len(y), batch_size):\n        b = idx[start:start + batch_size]\n        loss = model.step(X[b], y[b])\n        losses.append(loss)\n    return float(np.mean(losses))\n\n\ndef train_single_model(seed, X_tr, y_tr, X_va, y_va, u_va, new_dim):\n    \"\"\"Train one FM model with given seed, return best model and epoch count.\"\"\"\n    rng = np.random.default_rng(seed)\n    model = FM(new_dim, k=16, lr=0.001, l2=1e-6, seed=seed)\n\n    best_val = -1.0\n    best_weights = None\n    patience_left = PATIENCE\n    best_epoch = 0\n\n    for epoch in range(1, MAX_EPOCHS + 1):\n        tr_loss = run_epoch(model, X_tr, y_tr, rng)\n\n        val_scores = model.predict(X_va)\n        val_res = evaluate(u_va, y_va, val_scores)\n        val_primary = val_res['primary']\n\n        print(f\"  Seed {seed} Epoch {epoch:02d}  tr_loss={tr_loss:.4f}  \"\n              f\"val_primary={val_primary:.4f}\", flush=True)\n\n        if val_primary > best_val + 1e-6:\n            best_val = val_primary\n            best_weights = (\n                model.V.copy(),\n                model.W.copy(),\n                float(model.b)\n            )\n            patience_left = PATIENCE\n            best_epoch = epoch\n        else:\n            patience_left -= 1\n            if patience_left <= 0:\n                print(f\"  Seed {seed} early stop at epoch {epoch}, best was {best_epoch}\",\n                      flush=True)\n                break\n\n    # Restore best weights\n    model.V[:] = best_weights[0]\n    model.W[:] = best_weights[1]\n    model.b = best_weights[2]\n\n    return model, best_epoch\n\n\nif args.train_split == 'train':\n    # \u2500\u2500 Load random data for unbiased evaluation \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    rand_rows = load_random_valid(args.data_dir)\n    X_rand_base, y_rand, u_rand, _ = encode_like_train(splits['train'], rand_rows)\n\n    # Compute CTR features for random rows (use same maps from training)\n    rand_video_rates = make_bucket_feature(rand_rows, video_ctr_map, video_global_rate, key_idx=2)\n    rand_author_rates = make_bucket_feature(rand_rows, author_ctr_map, author_global_rate, key_idx=3)\n    rand_video_buckets = compute_buckets_from_rates(train_video_rates, rand_video_rates, N_BINS)\n    rand_author_buckets = compute_buckets_from_rates(train_author_rates, rand_author_rates, N_BINS)\n    X_rand = extend_features(X_rand_base, rand_video_buckets, rand_author_buckets)\n\n    # \u2500\u2500 Train N_SEEDS models and collect scores \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    va_rank_sum = np.zeros(len(y_va), dtype=np.float64)\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n    tr_rank_sum = np.zeros(len(y_tr), dtype=np.float64)\n    rand_rank_sum = np.zeros(len(y_rand), dtype=np.float64)\n\n    best_epochs = []\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} ===\", flush=True)\n        model, best_epoch = train_single_model(s, X_tr, y_tr, X_va, y_va, u_va, NEW_DIM)\n        best_epochs.append(best_epoch)\n\n        # Compute scores for each split\n        va_scores = model.predict(X_va)\n        te_scores = model.predict(X_te)\n        tr_scores = model.predict(X_tr)\n        rand_scores = model.predict(X_rand)\n\n        # Convert to within-user fractional ranks\n        va_ranks = within_user_ranks(u_va, va_scores)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        tr_ranks = within_user_ranks(u_tr, tr_scores)\n        rand_ranks = within_user_ranks(u_rand, rand_scores)\n\n        va_rank_sum += va_ranks\n        te_rank_sum += te_ranks\n        tr_rank_sum += tr_ranks\n        rand_rank_sum += rand_ranks\n\n    # Average ranks\n    va_final = va_rank_sum / N_SEEDS\n    te_final = te_rank_sum / N_SEEDS\n    tr_final = tr_rank_sum / N_SEEDS\n    rand_final = rand_rank_sum / N_SEEDS\n\n    print(f\"\\nBest epochs across seeds: {best_epochs}\", flush=True)\n\n    # \u2500\u2500 Evaluate \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    tr_res = evaluate(u_tr, y_tr, tr_final)\n    va_res = evaluate(u_va, y_va, va_final)\n\n    # Unbiased evaluation using averaged ranks\n    unbiased = unbiased_primary(\n        args.data_dir, splits['train'],\n        lambda rows: rand_final\n    )\n\n    print(f\"TRAIN_PRIMARY={tr_res['primary']:.4f}\")\n    print(f\"VAL_GAUC={va_res['GAUC']:.4f}\")\n    print(f\"VAL_NDCG5={va_res['nDCG@5']:.4f}\")\n    print(f\"VAL_PRIMARY={va_res['primary']:.4f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.4f}\")\n\n    # \u2500\u2500 Write submissions \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        splits['valid'], va_final\n    )\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )\n\nelse:\n    # train+valid mode: combine train and valid, train for fixed epoch count\n    # Need to recompute CTR features combining train+valid for proper features\n    # BUT: we must not recompute CTR maps from train+valid (would be leakage for test scoring)\n    # CTR maps remain from training only; just extend features for valid rows as done above\n\n    X_all = np.concatenate([X_tr, X_va], axis=0)\n    y_all = np.concatenate([y_tr, y_va], axis=0)\n    u_all = list(u_tr) + list(u_va)\n\n    FIXED_EPOCHS = 10  # conservative fixed schedule for train+valid\n\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} (train+valid) ===\", flush=True)\n        rng = np.random.default_rng(s)\n        model = FM(NEW_DIM, k=16, lr=0.001, l2=1e-6, seed=s)\n\n        for epoch in range(1, FIXED_EPOCHS + 1):\n            tr_loss = run_epoch(model, X_all, y_all, rng)\n            print(f\"  Seed {s} Epoch {epoch:02d}  tr_loss={tr_loss:.4f}\", flush=True)\n\n        te_scores = model.predict(X_te)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        te_rank_sum += te_ranks\n\n    te_final = te_rank_sum / N_SEEDS\n\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )", "plan": "", "spec": {"hypothesis": "Adding Bayesian-smoothed per-video and per-author historical long_view rates (bucketed into categorical features) to the FM on top of the 5-seed rank ensemble will increase validation primary by ~+0.0008, pushing the combined system to ~0.6036+", "mechanism": "The smoothed CTR features give the model a prior on how engaging each video/author is, computed from training history. This varies across the candidate videos within each user's impression list, so it is not constant-per-user and does contribute to within-user ranking. Bayesian smoothing prevents rare items from dominating while still allowing confident estimates for frequently-seen videos.", "evidence": {"task_structure": "Each user sees ~5 videos in the evaluation window; the within-user ranking depends on features that differ across those 5 videos. A video's historical engagement rate from training data varies across candidates and is a strong signal about its intrinsic quality, separate from the user-video interaction.", "previous_experiments": "The established findings explicitly state 'Bayesian-smoothed per-video and per-author long-view rate, bucketed: +0.0008' and that this composes with the ensembling result: 'those two together account for +0.0024 of a best-ever +0.0025'.", "literature": ""}, "target_metric": "both", "proposed_change": "Compute per-video and per-author Bayesian-smoothed long_view rates from training-only data. Bucket each into 10 equal-frequency bins. Add these two buckets as additional categorical feature columns to the FM input (extending the embedding table). Train the 5-seed ensemble with these 7 total features instead of 5.", "expected_result": "VAL_PRIMARY ~0.6036, delta ~+0.0008 over current best, combined with ensembling for total ~+0.0020 over baseline", "falsification_condition": "VAL_PRIMARY <= 0.6030, i.e., no improvement over the ensemble-only result of 0.6028", "risks": {"leakage": "Must compute CTR only from training rows (dates 0408-0421), never including validation/test rows. The smoothing toward global rate prevents overfitting to the training period.", "overfitting": "Bucketization and Bayesian smoothing reduce overfitting risk. With 7538 unique videos seen in training and sufficient smoothing strength, estimates should generalize.", "runtime": "Adding 2 feature columns is negligible overhead. The 5-seed ensemble already takes the bulk of compute time."}, "implementation_scope": ["solution.py"], "tags": ["feature-engineering", "fm", "ensembling", "bayesian-smoothing"], "candidates_considered": ["Bayesian-smoothed per-video and per-author long_view rate buckets added as FM features on top of the 5-seed ensemble \u2014 expected +0.0008, composes with ensembling for ~+0.0024 total", "Pairwise BPR ranking loss instead of BCE \u2014 ranking objective history shows 16% acceptance, median -0.0014, not compelling", "LightGBM with tabular features \u2014 gradient-boosted trees history shows 0% acceptance, median -0.0061, strongly negative track record"], "reflection": "Node 2 got +0.0012 with 5-seed ensemble rank averaging, just below the noise floor. The established findings say ensembling alone gives ~+0.0016 and Bayesian-smoothed CTR adds ~+0.0008, composing for +0.0024 total. The ensemble is in place; adding the smoothed CTR features is the next logical step that should push the combined effect above the noise floor.", "feature_provenance": null}, "gate_result": "passed", "review_verdict": "CLEAN", "review_reason": "", "train_primary": 0.671, "val_gauc": 0.6697, "val_ndcg5": 0.5371, "val_primary": 0.6034, "unbiased_val_primary": 0.3642, "seeds_averaged": 3, "seed_scores": [0.6034, 0.6034, 0.6034], "seed_unbiased_scores": [0.3642, 0.3642, 0.3642], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "Epoch 09  tr_loss=0.4857  val_primary=0.6021\n  Seed 2 Epoch 10  tr_loss=0.4828  val_primary=0.6014\n  Seed 2 Epoch 11  tr_loss=0.4798  val_primary=0.6011\n  Seed 2 early stop at epoch 11, best was 7\n\n=== Training seed 3 ===\n  Seed 3 Epoch 01  tr_loss=0.6220  val_primary=0.5874\n  Seed 3 Epoch 02  tr_loss=0.5405  val_primary=0.5959\n  Seed 3 Epoch 03  tr_loss=0.5117  val_primary=0.6003\n  Seed 3 Epoch 04  tr_loss=0.5013  val_primary=0.6016\n  Seed 3 Epoch 05  tr_loss=0.4968  val_primary=0.6019\n  Seed 3 Epoch 06  tr_loss=0.4934  val_primary=0.6019\n  Seed 3 Epoch 07  tr_loss=0.4905  val_primary=0.6019\n  Seed 3 Epoch 08  tr_loss=0.4876  val_primary=0.6013\n  Seed 3 Epoch 09  tr_loss=0.4844  val_primary=0.6006\n  Seed 3 early stop at epoch 9, best was 5\n\n=== Training seed 4 ===\n  Seed 4 Epoch 01  tr_loss=0.6211  val_primary=0.5903\n  Seed 4 Epoch 02  tr_loss=0.5363  val_primary=0.5971\n  Seed 4 Epoch 03  tr_loss=0.5095  val_primary=0.6004\n  Seed 4 Epoch 04  tr_loss=0.4998  val_primary=0.6010\n  Seed 4 Epoch 05  tr_loss=0.4946  val_primary=0.6020\n  Seed 4 Epoch 06  tr_loss=0.4908  val_primary=0.6025\n  Seed 4 Epoch 07  tr_loss=0.4873  val_primary=0.6024\n  Seed 4 Epoch 08  tr_loss=0.4837  val_primary=0.6016\n  Seed 4 Epoch 09  tr_loss=0.4798  val_primary=0.6017\n  Seed 4 Epoch 10  tr_loss=0.4758  val_primary=0.6003\n  Seed 4 early stop at epoch 10, best was 6\n\nBest epochs across seeds: [8, 4, 7, 5, 6]\nTRAIN_PRIMARY=0.6710\nVAL_GAUC=0.6697\nVAL_NDCG5=0.5371\nVAL_PRIMARY=0.6034\nUNBIASED_PRIMARY=0.3642\n", "stderr_tail": "", "exec_time": 358.1619945419952, "submission_ok": true, "accepted": true, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 21324, "tokens_out": 11843, "wall_seconds": 1194.633685000008}
{"id": 4, "parent_id": 3, "operation": "improve", "hypothesis": "Replacing pointwise BCE with a BPR pairwise loss (sampling positive/negative pairs within each user per batch) will improve GAUC by directly optimizing the pairwise ranking objective, producing a gain of >0.002 over the current best", "code": "#!/usr/bin/env python3\n\"\"\"\nIteration 3: Replace pointwise BCE with BPR pairwise loss.\n\nChanges from iteration 2:\n- Instead of pointwise BCE, sample within-user (positive, negative) pairs\n  and apply BPR loss = -log(sigmoid(score_pos - score_neg))\n- Uses FM's logits() to get scores, then manually computes gradients\n- Everything else identical: 5-seed ensemble, rank averaging, CTR features\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\n\n# \u2500\u2500 CLI \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nparser = argparse.ArgumentParser()\nparser.add_argument('--data_dir', required=True)\nparser.add_argument('--out_dir', required=True)\nparser.add_argument('--seed', type=int, default=0)\nparser.add_argument('--train_split', default='train',\n                    choices=['train', 'train+valid'])\nargs = parser.parse_args()\n\nos.makedirs(args.out_dir, exist_ok=True)\n\n# \u2500\u2500 Imports \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfrom data import load, encode, FIELDS\nfrom baseline import FM\nfrom evaluate import evaluate\nimport submit\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n# \u2500\u2500 Data \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nsplits = load(args.data_dir)\n\nenc, dim = encode(splits)\nX_tr_base, y_tr, u_tr = enc['train']\nX_va_base, y_va, u_va = enc['valid']\nX_te_base, y_te, u_te = enc['test']\n\n# \u2500\u2500 Compute Bayesian-smoothed CTR features from training data only \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nGLOBAL_MEAN_PRIOR_STRENGTH = 50\n\ndef compute_bayesian_ctr(train_rows, key_idx, label_idx=6):\n    counts = {}\n    for row in train_rows:\n        key = row[key_idx]\n        label = row[label_idx]\n        if key not in counts:\n            counts[key] = [0, 0]\n        counts[key][0] += 1\n        counts[key][1] += label\n\n    total_all = sum(v[0] for v in counts.values())\n    pos_all = sum(v[1] for v in counts.values())\n    global_rate = pos_all / total_all if total_all > 0 else 0.5\n\n    alpha = GLOBAL_MEAN_PRIOR_STRENGTH\n    smoothed = {}\n    for key, (total, pos) in counts.items():\n        smoothed[key] = (pos + alpha * global_rate) / (total + alpha)\n\n    return smoothed, global_rate\n\n\ndef make_bucket_feature(rows, ctr_map, global_rate, key_idx):\n    rates = np.array([\n        ctr_map.get(row[key_idx], global_rate)\n        for row in rows\n    ], dtype=np.float64)\n    return rates\n\n\ndef compute_buckets_from_rates(train_rates, query_rates, n_bins=10):\n    quantiles = np.linspace(0, 100, n_bins + 1)\n    edges = np.percentile(train_rates, quantiles)\n    edges = np.unique(edges)\n    buckets = np.searchsorted(edges[1:-1], query_rates, side='right')\n    buckets = np.clip(buckets, 0, n_bins - 1)\n    return buckets.astype(np.int32)\n\n\ntrain_rows = splits['train']\nvalid_rows = splits['valid']\ntest_rows = splits['test']\n\nprint(\"Computing video CTR map...\", flush=True)\nvideo_ctr_map, video_global_rate = compute_bayesian_ctr(train_rows, key_idx=2)\nprint(f\"  {len(video_ctr_map)} videos, global rate={video_global_rate:.4f}\", flush=True)\n\nprint(\"Computing author CTR map...\", flush=True)\nauthor_ctr_map, author_global_rate = compute_bayesian_ctr(train_rows, key_idx=3)\nprint(f\"  {len(author_ctr_map)} authors, global rate={author_global_rate:.4f}\", flush=True)\n\nN_BINS = 10\n\ntrain_video_rates = make_bucket_feature(train_rows, video_ctr_map, video_global_rate, key_idx=2)\ntrain_author_rates = make_bucket_feature(train_rows, author_ctr_map, author_global_rate, key_idx=3)\n\nvalid_video_rates = make_bucket_feature(valid_rows, video_ctr_map, video_global_rate, key_idx=2)\nvalid_author_rates = make_bucket_feature(valid_rows, author_ctr_map, author_global_rate, key_idx=3)\n\ntest_video_rates = make_bucket_feature(test_rows, video_ctr_map, video_global_rate, key_idx=2)\ntest_author_rates = make_bucket_feature(test_rows, author_ctr_map, author_global_rate, key_idx=3)\n\ntrain_video_buckets = compute_buckets_from_rates(train_video_rates, train_video_rates, N_BINS)\ntrain_author_buckets = compute_buckets_from_rates(train_author_rates, train_author_rates, N_BINS)\n\nvalid_video_buckets = compute_buckets_from_rates(train_video_rates, valid_video_rates, N_BINS)\nvalid_author_buckets = compute_buckets_from_rates(train_author_rates, valid_author_rates, N_BINS)\n\ntest_video_buckets = compute_buckets_from_rates(train_video_rates, test_video_rates, N_BINS)\ntest_author_buckets = compute_buckets_from_rates(train_author_rates, test_author_rates, N_BINS)\n\nVIDEO_CTR_OFFSET = dim\nAUTHOR_CTR_OFFSET = dim + N_BINS\nNEW_DIM = dim + N_BINS + N_BINS\n\nprint(f\"Original embedding dim: {dim}, New dim: {NEW_DIM}\", flush=True)\n\ndef extend_features(X_base, video_buckets, author_buckets):\n    vid_col = (video_buckets + VIDEO_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    auth_col = (author_buckets + AUTHOR_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    return np.concatenate([X_base, vid_col, auth_col], axis=1)\n\nX_tr = extend_features(X_tr_base, train_video_buckets, train_author_buckets)\nX_va = extend_features(X_va_base, valid_video_buckets, valid_author_buckets)\nX_te = extend_features(X_te_base, test_video_buckets, test_author_buckets)\n\nprint(f\"Feature matrix shapes: train={X_tr.shape}, valid={X_va.shape}, test={X_te.shape}\", flush=True)\n\n# \u2500\u2500 Hyperparameters \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nBATCH = 8192       # number of pairs per BPR batch\nPATIENCE = 4\nMAX_EPOCHS = 50\nN_SEEDS = 5\n\n# \u2500\u2500 Helper: within-user fractional ranks \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef within_user_ranks(users, scores):\n    user_arr = np.array(users)\n    ranks = np.zeros(len(scores), dtype=np.float64)\n    unique_users = np.unique(user_arr)\n    for u in unique_users:\n        mask = user_arr == u\n        s = scores[mask]\n        n = mask.sum()\n        if n == 1:\n            ranks[mask] = 0.5\n            continue\n        order = np.argsort(s)\n        r = np.empty(n, dtype=np.float64)\n        r[order] = np.arange(n, dtype=np.float64)\n        r = r / (n - 1)\n        ranks[mask] = r\n    return ranks\n\n\n# \u2500\u2500 BPR training \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef build_user_pos_neg_index(users, labels):\n    \"\"\"\n    Build per-user lists of positive and negative indices.\n    Returns: user_pos (dict user -> list of row indices with label=1)\n             user_neg (dict user -> list of row indices with label=0)\n             eligible_users (list of users with both pos and neg)\n    \"\"\"\n    user_pos = {}\n    user_neg = {}\n    for i, (u, lbl) in enumerate(zip(users, labels)):\n        if lbl > 0.5:\n            user_pos.setdefault(u, []).append(i)\n        else:\n            user_neg.setdefault(u, []).append(i)\n\n    eligible = [u for u in user_pos if u in user_neg]\n    return user_pos, user_neg, eligible\n\n\ndef sample_bpr_pairs(user_pos, user_neg, eligible_users, n_pairs, rng):\n    \"\"\"\n    Sample n_pairs (positive_idx, negative_idx) within-user pairs uniformly.\n    Returns arrays pos_idx, neg_idx of shape (n_pairs,).\n    \"\"\"\n    pos_idx = np.empty(n_pairs, dtype=np.int64)\n    neg_idx = np.empty(n_pairs, dtype=np.int64)\n    n_eligible = len(eligible_users)\n\n    for i in range(n_pairs):\n        u = eligible_users[rng.integers(0, n_eligible)]\n        p = user_pos[u][rng.integers(0, len(user_pos[u]))]\n        n = user_neg[u][rng.integers(0, len(user_neg[u]))]\n        pos_idx[i] = p\n        neg_idx[i] = n\n\n    return pos_idx, neg_idx\n\n\ndef bpr_step(model, X, pos_idx, neg_idx, lr=0.001, l2=1e-6):\n    \"\"\"\n    One BPR update step.\n    Computes loss = -mean(log(sigmoid(z_pos - z_neg)))\n    and updates model weights via SGD-style gradient.\n    \n    We implement this by leveraging FM.logits() and manual gradient computation.\n    The FM score for a sample is: z = b + sum_f(W[x_f]) + sum_{f<g}(V[x_f] . V[x_g])\n    \n    For BPR, gradient wrt each parameter p:\n      dL/dp = -sigmoid(-diff) * (dz_pos/dp - dz_neg/dp)\n    where diff = z_pos - z_neg\n    \"\"\"\n    X_pos = X[pos_idx]\n    X_neg = X[neg_idx]\n    B = len(pos_idx)\n    \n    # Get scores and intermediate values\n    z_pos, E_pos, S_pos = model.logits(X_pos)  # E: (B, F, k), S: (B, k)\n    z_neg, E_neg, S_neg = model.logits(X_neg)\n    \n    diff = z_pos - z_neg  # (B,)\n    # sigmoid(-diff) = 1 - sigmoid(diff)\n    sig_neg_diff = 1.0 / (1.0 + np.exp(diff))  # gradient multiplier: -sigmoid(-diff) = sig_neg_diff - 1... \n    # Actually: d/dz_pos [-log(sigmoid(diff))] = -sigmoid(-diff) = -(1-sigmoid(diff)) = sigmoid(diff)-1\n    # So grad_multiplier for z_pos = sigmoid(diff) - 1 = -sigmoid(-diff)\n    # And grad_multiplier for z_neg = +sigmoid(-diff)\n    \n    # grad_multiplier: shape (B,)\n    # dL/dz_pos = (sigmoid(diff) - 1) / B = -sig_neg_diff / B\n    # dL/dz_neg = sig_neg_diff / B\n    \n    gm_pos = -sig_neg_diff / B  # (B,)\n    gm_neg = sig_neg_diff / B   # (B,)\n    \n    # Compute loss for monitoring\n    loss = -np.mean(np.log(np.maximum(1.0 - sig_neg_diff, 1e-15)))\n    \n    # \u2500\u2500 Gradient for bias b \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    db = np.sum(gm_pos) + np.sum(gm_neg)\n    \n    # \u2500\u2500 Gradient for W (first-order weights) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    # dz/dW[x_f] = 1 for each feature index x_f in the row\n    # X_pos: (B, F), each row has F feature indices\n    # We need to accumulate gradient: for each feature index, sum gm_pos over rows containing it\n    F = X_pos.shape[1]\n    k = model.V.shape[1]\n    \n    dW = np.zeros_like(model.W)  # shape: (dim,)\n    # Scatter add for positive items\n    for f in range(F):\n        np.add.at(dW, X_pos[:, f], gm_pos)\n        np.add.at(dW, X_neg[:, f], gm_neg)\n    \n    # \u2500\u2500 Gradient for V (embeddings) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    # FM interaction score: 0.5 * (||sum_f V[x_f]||^2 - sum_f ||V[x_f]||^2)\n    # dz/dV[x_f] = S - V[x_f]  where S = sum_g V[x_g]\n    # S_pos: (B, k) = sum of embeddings for each positive row\n    # E_pos: (B, F, k) = individual embeddings for each feature\n    \n    dV = np.zeros_like(model.V)  # shape: (dim, k)\n    \n    # For positive rows:\n    # dL/dV[x_f] += gm_pos[b] * (S_pos[b] - E_pos[b, f])\n    for f in range(F):\n        # grad_f = gm_pos[:, None] * (S_pos - E_pos[:, f, :])  shape: (B, k)\n        grad_f_pos = gm_pos[:, None] * (S_pos - E_pos[:, f, :])\n        np.add.at(dV, X_pos[:, f], grad_f_pos)\n        \n        grad_f_neg = gm_neg[:, None] * (S_neg - E_neg[:, f, :])\n        np.add.at(dV, X_neg[:, f], grad_f_neg)\n    \n    # \u2500\u2500 L2 regularization \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    # Only regularize the features that appear in this batch\n    unique_pos = np.unique(X_pos)\n    unique_neg = np.unique(X_neg)\n    all_unique = np.unique(np.concatenate([unique_pos, unique_neg]))\n    \n    dW[all_unique] += l2 * model.W[all_unique]\n    dV[all_unique] += l2 * model.V[all_unique]\n    \n    # \u2500\u2500 Adam update (use model's internal Adam state if available, else SGD) \u2500\u2500\n    # FM class uses Adam internally via step(), but we need manual control here.\n    # We'll use the model's Adam moment estimates directly.\n    # Check if model has Adam state; FM baseline stores t, m_V, v_V, m_W, v_W, m_b, v_b\n    \n    t = model.t + 1\n    model.t = t\n    beta1, beta2, eps = 0.9, 0.999, 1e-8\n    \n    # Update b\n    model.m_b = beta1 * model.m_b + (1 - beta1) * db\n    model.v_b = beta2 * model.v_b + (1 - beta2) * db * db\n    mb_hat = model.m_b / (1 - beta1 ** t)\n    vb_hat = model.v_b / (1 - beta2 ** t)\n    model.b -= lr * mb_hat / (np.sqrt(vb_hat) + eps)\n    \n    # Update W\n    model.m_W += (1 - beta1) * (dW - model.m_W)  # This is wrong, let me fix\n    # Actually FM stores: m_W = beta1 * m_W + (1-beta1) * grad  -- standard Adam\n    # Let me redo this properly\n    model.m_W = beta1 * model.m_W + (1 - beta1) * dW\n    model.v_W = beta2 * model.v_W + (1 - beta2) * dW * dW\n    mW_hat = model.m_W / (1 - beta1 ** t)\n    vW_hat = model.v_W / (1 - beta2 ** t)\n    model.W -= lr * mW_hat / (np.sqrt(vW_hat) + eps)\n    \n    # Update V\n    model.m_V = beta1 * model.m_V + (1 - beta1) * dV\n    model.v_V = beta2 * model.v_V + (1 - beta2) * dV * dV\n    mV_hat = model.m_V / (1 - beta1 ** t)\n    vV_hat = model.v_V / (1 - beta2 ** t)\n    model.V -= lr * mV_hat / (np.sqrt(vV_hat) + eps)\n    \n    return loss\n\n\ndef run_bpr_epoch(model, X, users, labels, rng, batch_size=BATCH, lr=0.001, l2=1e-6):\n    \"\"\"\n    One BPR epoch: sample pairs and update.\n    Number of batches = ceil(n_positives / batch_size) to match roughly one pass.\n    \"\"\"\n    user_pos, user_neg, eligible = build_user_pos_neg_index(users, labels)\n    \n    if len(eligible) == 0:\n        return 0.0\n    \n    # Number of positive samples in training\n    n_pos = sum(len(v) for v in user_pos.values())\n    n_batches = max(1, n_pos // batch_size)\n    \n    losses = []\n    for _ in range(n_batches):\n        pos_idx, neg_idx = sample_bpr_pairs(user_pos, user_neg, eligible, batch_size, rng)\n        loss = bpr_step(model, X, pos_idx, neg_idx, lr=lr, l2=l2)\n        losses.append(loss)\n    \n    return float(np.mean(losses))\n\n\ndef train_single_model_bpr(seed, X_tr, y_tr, u_tr, X_va, y_va, u_va, new_dim):\n    \"\"\"Train one FM model with BPR loss, return best model and epoch count.\"\"\"\n    rng = np.random.default_rng(seed)\n    model = FM(new_dim, k=16, lr=0.001, l2=1e-6, seed=seed)\n    \n    # Build user index once\n    users_arr = np.array(u_tr)\n    labels_arr = y_tr\n    \n    best_val = -1.0\n    best_weights = None\n    patience_left = PATIENCE\n    best_epoch = 0\n\n    for epoch in range(1, MAX_EPOCHS + 1):\n        tr_loss = run_bpr_epoch(model, X_tr, users_arr, labels_arr, rng,\n                                batch_size=BATCH, lr=0.001, l2=1e-6)\n\n        val_scores = model.predict(X_va)\n        val_res = evaluate(u_va, y_va, val_scores)\n        val_primary = val_res['primary']\n\n        print(f\"  Seed {seed} Epoch {epoch:02d}  bpr_loss={tr_loss:.4f}  \"\n              f\"val_primary={val_primary:.4f}\", flush=True)\n\n        if val_primary > best_val + 1e-6:\n            best_val = val_primary\n            best_weights = (\n                model.V.copy(),\n                model.W.copy(),\n                float(model.b),\n                model.m_V.copy(),\n                model.v_V.copy(),\n                model.m_W.copy(),\n                model.v_W.copy(),\n                float(model.m_b),\n                float(model.v_b),\n                int(model.t),\n            )\n            patience_left = PATIENCE\n            best_epoch = epoch\n        else:\n            patience_left -= 1\n            if patience_left <= 0:\n                print(f\"  Seed {seed} early stop at epoch {epoch}, best was {best_epoch}\",\n                      flush=True)\n                break\n\n    # Restore best weights\n    model.V[:] = best_weights[0]\n    model.W[:] = best_weights[1]\n    model.b = best_weights[2]\n\n    return model, best_epoch\n\n\nif args.train_split == 'train':\n    # \u2500\u2500 Load random data for unbiased evaluation \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    rand_rows = load_random_valid(args.data_dir)\n    X_rand_base, y_rand, u_rand, _ = encode_like_train(splits['train'], rand_rows)\n\n    rand_video_rates = make_bucket_feature(rand_rows, video_ctr_map, video_global_rate, key_idx=2)\n    rand_author_rates = make_bucket_feature(rand_rows, author_ctr_map, author_global_rate, key_idx=3)\n    rand_video_buckets = compute_buckets_from_rates(train_video_rates, rand_video_rates, N_BINS)\n    rand_author_buckets = compute_buckets_from_rates(train_author_rates, rand_author_rates, N_BINS)\n    X_rand = extend_features(X_rand_base, rand_video_buckets, rand_author_buckets)\n\n    # \u2500\u2500 Train N_SEEDS models and collect scores \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    va_rank_sum = np.zeros(len(y_va), dtype=np.float64)\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n    tr_rank_sum = np.zeros(len(y_tr), dtype=np.float64)\n    rand_rank_sum = np.zeros(len(y_rand), dtype=np.float64)\n\n    best_epochs = []\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} ===\", flush=True)\n        model, best_epoch = train_single_model_bpr(\n            s, X_tr, y_tr, u_tr, X_va, y_va, u_va, NEW_DIM\n        )\n        best_epochs.append(best_epoch)\n\n        va_scores = model.predict(X_va)\n        te_scores = model.predict(X_te)\n        tr_scores = model.predict(X_tr)\n        rand_scores = model.predict(X_rand)\n\n        va_ranks = within_user_ranks(u_va, va_scores)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        tr_ranks = within_user_ranks(u_tr, tr_scores)\n        rand_ranks = within_user_ranks(u_rand, rand_scores)\n\n        va_rank_sum += va_ranks\n        te_rank_sum += te_ranks\n        tr_rank_sum += tr_ranks\n        rand_rank_sum += rand_ranks\n\n    va_final = va_rank_sum / N_SEEDS\n    te_final = te_rank_sum / N_SEEDS\n    tr_final = tr_rank_sum / N_SEEDS\n    rand_final = rand_rank_sum / N_SEEDS\n\n    print(f\"\\nBest epochs across seeds: {best_epochs}\", flush=True)\n\n    tr_res = evaluate(u_tr, y_tr, tr_final)\n    va_res = evaluate(u_va, y_va, va_final)\n\n    unbiased = unbiased_primary(\n        args.data_dir, splits['train'],\n        lambda rows: rand_final\n    )\n\n    print(f\"TRAIN_PRIMARY={tr_res['primary']:.4f}\")\n    print(f\"VAL_GAUC={va_res['GAUC']:.4f}\")\n    print(f\"VAL_NDCG5={va_res['nDCG@5']:.4f}\")\n    print(f\"VAL_PRIMARY={va_res['primary']:.4f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.4f}\")\n\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        splits['valid'], va_final\n    )\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )\n\nelse:\n    # train+valid mode\n    X_all = np.concatenate([X_tr, X_va], axis=0)\n    y_all = np.concatenate([y_tr, y_va], axis=0)\n    u_all = list(u_tr) + list(u_va)\n\n    FIXED_EPOCHS = 7  # median of best epochs from train run\n\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} (train+valid) ===\", flush=True)\n        rng = np.random.default_rng(s)\n        model = FM(NEW_DIM, k=16, lr=0.001, l2=1e-6, seed=s)\n        users_all_arr = np.array(u_all)\n\n        for epoch in range(1, FIXED_EPOCHS + 1):\n            tr_loss = run_bpr_epoch(model, X_all, users_all_arr, y_all, rng,\n                                    batch_size=BATCH, lr=0.001, l2=1e-6)\n            print(f\"  Seed {s} Epoch {epoch:02d}  bpr_loss={tr_loss:.4f}\", flush=True)\n\n        te_scores = model.predict(X_te)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        te_rank_sum += te_ranks\n\n    te_final = te_rank_sum / N_SEEDS\n\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )", "plan": "", "spec": {"hypothesis": "Replacing pointwise BCE with a BPR pairwise loss (sampling positive/negative pairs within each user per batch) will improve GAUC by directly optimizing the pairwise ranking objective, producing a gain of >0.002 over the current best", "mechanism": "GAUC is per-user AUC = fraction of (positive, negative) pairs where the positive scores higher. BPR loss minimizes -log(sigmoid(score_pos - score_neg)) for within-user pairs, which is the exact pairwise objective GAUC measures. Pointwise BCE does not see the relative ordering of items within a user's list, only absolute label prediction. With ~5 items/user and 1-2 positives, each user contributes ~3-6 pairs; BPR directly trains the model to get these pairs right.", "evidence": {"task_structure": "GAUC is a pairwise metric computed within users; with ~5 impressions/user the evaluation is almost entirely determined by pairwise ordering of 1-2 positives against 3-4 negatives within each user's tiny list", "previous_experiments": "Node 0-3 all use pointwise BCE; the ranking objective category has 4 accepted runs out of 25 with best delta +0.0015, but those attempts were on older configurations without the CTR features and rank averaging that our current best uses", "literature": "[W2512971201] YouTube DNN recommendation paper discusses that direct optimization of ranking metrics outperforms pointwise approaches for ranking tasks. The BPR mechanism is well-established for within-user pairwise ranking."}, "target_metric": "GAUC", "proposed_change": "Replace pointwise BCE training with BPR pairwise loss: for each minibatch, sample within-user (positive, negative) pairs and apply BPR loss = -log(sigmoid(score_pos - score_neg)). Keep everything else identical (5-seed ensemble, rank averaging, CTR features, FM architecture).", "expected_result": "VAL_PRIMARY improvement of ~0.002-0.004 over current best 0.6034, primarily through GAUC increase", "falsification_condition": "If VAL_PRIMARY <= 0.6034 + 0.002 = 0.6054 after training, the BPR objective does not help in this configuration. If TRAIN_PRIMARY increases substantially more than VAL_PRIMARY, it's overfitting.", "risks": {"leakage": "None - BPR only uses within-user pairs from training labels, no future information", "overfitting": "Moderate - pairwise sampling concentrates gradient on hard pairs, could overfit to training user patterns. Mitigated by L2 regularization and early stopping on validation primary", "runtime": "Low - BPR sampling adds O(batch) overhead per step but does not change the number of epochs or seeds"}, "implementation_scope": ["solution.py"], "tags": ["ranking-objective", "bpr", "pairwise-loss", "fm"], "candidates_considered": ["BPR pairwise ranking loss: train with within-user positive/negative pairs to directly optimize GAUC; theoretically aligned with the metric, not yet tested on our best configuration", "Hour-of-day as bucketed categorical feature: extract hour from hourmin in raw logs, ~5.5pp range in long_view rate across hours, varies within user's multi-day eval window", "Increase ensemble size from 5 to 10 seeds: more variance reduction, but doubles runtime and has diminishing returns beyond 5 seeds"], "reflection": "Node 3 reached 0.6034 (+0.0018 over baseline) with Bayesian CTR features + 5-seed ensemble + rank averaging. The gain is real but smaller than the expected +0.0024 from the established findings. The FM is trained with pointwise BCE but evaluated on ranking metrics (GAUC = pairwise AUC). A pairwise ranking loss directly optimizes what GAUC measures. Prior attempts at ranking objectives had mixed results (median -0.0014, 4/25 accepted), but those were likely tested without the ensemble + CTR features that are now in our best model.", "feature_provenance": null}, "gate_result": "passed", "review_verdict": "CLEAN", "review_reason": "", "train_primary": null, "val_gauc": null, "val_ndcg5": null, "val_primary": null, "unbiased_val_primary": null, "seeds_averaged": 1, "seed_scores": [], "seed_unbiased_scores": [], "is_buggy": true, "buggy_reason": "exception", "exception_type": "AttributeError", "stdout_tail": "Computing video CTR map...\n  7538 videos, global rate=0.3366\nComputing author CTR map...\n  6482 authors, global rate=0.3366\nOriginal embedding dim: 40260, New dim: 40280\nFeature matrix shapes: train=(1141112, 7), valid=(124909, 7), test=(170588, 7)\n\n=== Training seed 0 ===\n", "stderr_tail": "Traceback (most recent call last):\n  File \"runs/v10/nodes/node_4/solution.py\", line 416, in <module>\n    model, best_epoch = train_single_model_bpr(\n                        ~~~~~~~~~~~~~~~~~~~~~~^\n        s, X_tr, y_tr, u_tr, X_va, y_va, u_va, NEW_DIM\n        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n    )\n    ^\n  File \"runs/v10/nodes/node_4/solution.py\", line 354, in train_single_model_bpr\n    tr_loss = run_bpr_epoch(model, X_tr, users_arr, labels_arr, rng,\n                            batch_size=BATCH, lr=0.001, l2=1e-6)\n  File \"runs/v10/nodes/node_4/solution.py\", line 333, in run_bpr_epoch\n    loss = bpr_step(model, X, pos_idx, neg_idx, lr=lr, l2=l2)\n  File \"runs/v10/nodes/node_4/solution.py\", line 290, in bpr_step\n    model.m_b = beta1 * model.m_b + (1 - beta1) * db\n                        ^^^^^^^^^\nAttributeError: 'FM' object has no attribute 'm_b'", "exec_time": 10.88474499998847, "submission_ok": false, "accepted": false, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 37994, "tokens_out": 26137, "wall_seconds": 281.94296524999663}
{"id": 5, "parent_id": 3, "operation": "improve", "hypothesis": "", "code": "", "plan": "", "spec": null, "gate_result": "", "review_verdict": "", "review_reason": "", "train_primary": null, "val_gauc": null, "val_ndcg5": null, "val_primary": null, "unbiased_val_primary": null, "seeds_averaged": 1, "seed_scores": [], "seed_unbiased_scores": [], "is_buggy": true, "buggy_reason": "llm_failure", "exception_type": "LLMError", "stdout_tail": "", "stderr_tail": "LLM failed after 5 attempts: Connection error.", "exec_time": 0.0, "submission_ok": false, "accepted": false, "reflection": "", "recovery_action": "fall back to the best node next iteration", "debug_depth": 0, "tokens_in": 0, "tokens_out": 0, "wall_seconds": 79.37481145799393}
{"id": 6, "parent_id": 3, "operation": "improve", "hypothesis": "Replacing pointwise BCE with BPR pairwise loss \u2014 sampling within-user (positive, negative) pairs and training on sigmoid(score_pos - score_neg) \u2014 will improve GAUC and thus validation primary by optimizing the same objective the metric measures.", "code": "#!/usr/bin/env python3\n\"\"\"\nIteration 4: Replace pointwise BCE with BPR pairwise loss.\n\nChanges from iteration 2/3:\n- BPR pairwise training: sample (pos, neg) pairs within each minibatch per user\n- Optimize sigmoid(score_pos - score_neg) directly, which aligns with GAUC objective\n- Keep: 7 features, 5-seed ensemble, rank averaging, Bayesian-smoothed CTR buckets\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\n\n# \u2500\u2500 CLI \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nparser = argparse.ArgumentParser()\nparser.add_argument('--data_dir', required=True)\nparser.add_argument('--out_dir', required=True)\nparser.add_argument('--seed', type=int, default=0)\nparser.add_argument('--train_split', default='train',\n                    choices=['train', 'train+valid'])\nargs = parser.parse_args()\n\nos.makedirs(args.out_dir, exist_ok=True)\n\n# \u2500\u2500 Imports \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nfrom data import load, encode, FIELDS\nfrom evaluate import evaluate\nimport submit\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n# \u2500\u2500 Data \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nsplits = load(args.data_dir)\n\nenc, dim = encode(splits)\nX_tr_base, y_tr, u_tr = enc['train']\nX_va_base, y_va, u_va = enc['valid']\nX_te_base, y_te, u_te = enc['test']\n\n# \u2500\u2500 Compute Bayesian-smoothed CTR features from training data only \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nGLOBAL_MEAN_PRIOR_STRENGTH = 50\n\ndef compute_bayesian_ctr(train_rows, key_idx, label_idx=6):\n    counts = {}\n    for row in train_rows:\n        key = row[key_idx]\n        label = row[label_idx]\n        if key not in counts:\n            counts[key] = [0, 0]\n        counts[key][0] += 1\n        counts[key][1] += label\n\n    total_all = sum(v[0] for v in counts.values())\n    pos_all = sum(v[1] for v in counts.values())\n    global_rate = pos_all / total_all if total_all > 0 else 0.5\n\n    alpha = GLOBAL_MEAN_PRIOR_STRENGTH\n    smoothed = {}\n    for key, (total, pos) in counts.items():\n        smoothed[key] = (pos + alpha * global_rate) / (total + alpha)\n\n    return smoothed, global_rate\n\n\ndef make_bucket_feature(rows, ctr_map, global_rate, key_idx):\n    rates = np.array([\n        ctr_map.get(row[key_idx], global_rate)\n        for row in rows\n    ], dtype=np.float64)\n    return rates\n\n\ndef compute_buckets_from_rates(train_rates, query_rates, n_bins=10):\n    quantiles = np.linspace(0, 100, n_bins + 1)\n    edges = np.percentile(train_rates, quantiles)\n    edges = np.unique(edges)\n    buckets = np.searchsorted(edges[1:-1], query_rates, side='right')\n    buckets = np.clip(buckets, 0, n_bins - 1)\n    return buckets.astype(np.int32)\n\n\ntrain_rows = splits['train']\nvalid_rows = splits['valid']\ntest_rows = splits['test']\n\nprint(\"Computing video CTR map...\", flush=True)\nvideo_ctr_map, video_global_rate = compute_bayesian_ctr(train_rows, key_idx=2)\nprint(f\"  {len(video_ctr_map)} videos, global rate={video_global_rate:.4f}\", flush=True)\n\nprint(\"Computing author CTR map...\", flush=True)\nauthor_ctr_map, author_global_rate = compute_bayesian_ctr(train_rows, key_idx=3)\nprint(f\"  {len(author_ctr_map)} authors, global rate={author_global_rate:.4f}\", flush=True)\n\nN_BINS = 10\n\ntrain_video_rates = make_bucket_feature(train_rows, video_ctr_map, video_global_rate, key_idx=2)\ntrain_author_rates = make_bucket_feature(train_rows, author_ctr_map, author_global_rate, key_idx=3)\n\nvalid_video_rates = make_bucket_feature(valid_rows, video_ctr_map, video_global_rate, key_idx=2)\nvalid_author_rates = make_bucket_feature(valid_rows, author_ctr_map, author_global_rate, key_idx=3)\n\ntest_video_rates = make_bucket_feature(test_rows, video_ctr_map, video_global_rate, key_idx=2)\ntest_author_rates = make_bucket_feature(test_rows, author_ctr_map, author_global_rate, key_idx=3)\n\ntrain_video_buckets = compute_buckets_from_rates(train_video_rates, train_video_rates, N_BINS)\ntrain_author_buckets = compute_buckets_from_rates(train_author_rates, train_author_rates, N_BINS)\n\nvalid_video_buckets = compute_buckets_from_rates(train_video_rates, valid_video_rates, N_BINS)\nvalid_author_buckets = compute_buckets_from_rates(train_author_rates, valid_author_rates, N_BINS)\n\ntest_video_buckets = compute_buckets_from_rates(train_video_rates, test_video_rates, N_BINS)\ntest_author_buckets = compute_buckets_from_rates(train_author_rates, test_author_rates, N_BINS)\n\nVIDEO_CTR_OFFSET = dim\nAUTHOR_CTR_OFFSET = dim + N_BINS\nNEW_DIM = dim + N_BINS + N_BINS\n\nprint(f\"Original embedding dim: {dim}, New dim: {NEW_DIM}\", flush=True)\n\n\ndef extend_features(X_base, video_buckets, author_buckets):\n    vid_col = (video_buckets + VIDEO_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    auth_col = (author_buckets + AUTHOR_CTR_OFFSET).reshape(-1, 1).astype(np.int32)\n    return np.concatenate([X_base, vid_col, auth_col], axis=1)\n\n\nX_tr = extend_features(X_tr_base, train_video_buckets, train_author_buckets)\nX_va = extend_features(X_va_base, valid_video_buckets, valid_author_buckets)\nX_te = extend_features(X_te_base, test_video_buckets, test_author_buckets)\n\nprint(f\"Feature matrix shapes: train={X_tr.shape}, valid={X_va.shape}, test={X_te.shape}\", flush=True)\n\n# \u2500\u2500 FM model with BPR training \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n# We implement our own FM with Adam optimizer to support BPR loss\n# FM score: b + sum_f(W[f]*x[f]) + sum_{f<g}(V[f]\u00b7V[g])\n# Using the kernel trick: interaction = 0.5*(||sum_f V[f]||^2 - sum_f ||V[f]||^2)\n\nclass FMModel:\n    \"\"\"\n    Factorization Machine with Adam optimizer.\n    Supports both pointwise BCE (for warm-up) and BPR pairwise training.\n    \"\"\"\n    def __init__(self, n_features, k=16, lr=0.001, l2=1e-6, seed=0):\n        rng = np.random.default_rng(seed)\n        self.k = k\n        self.lr = lr\n        self.l2 = l2\n        self.n_features = n_features\n\n        # Parameters\n        self.V = rng.normal(0, 0.01, (n_features, k)).astype(np.float32)\n        self.W = np.zeros(n_features, dtype=np.float32)\n        self.b = np.float32(0.0)\n\n        # Adam state for V\n        self.m_V = np.zeros_like(self.V)\n        self.v_V = np.zeros_like(self.V)\n        self.m_W = np.zeros_like(self.W)\n        self.v_W = np.zeros_like(self.W)\n        self.m_b = 0.0\n        self.v_b = 0.0\n\n        self.t = 0  # Adam step count\n        self.beta1 = 0.9\n        self.beta2 = 0.999\n        self.eps = 1e-8\n\n    def _fm_score(self, X):\n        \"\"\"\n        Compute FM scores for rows in X.\n        X: int32 array (B, F) of feature indices\n        Returns: (scores, sum_emb, sq_sum_emb) for gradient computation\n        \"\"\"\n        B, F = X.shape\n        # First order\n        scores = np.full(B, self.b, dtype=np.float64)\n        for f in range(F):\n            idx = X[:, f]\n            scores += self.W[idx]\n\n        # Second order interaction via kernel trick\n        # sum_emb[b] = sum_f V[X[b,f]] shape (B, k)\n        sum_emb = np.zeros((B, self.k), dtype=np.float64)\n        sq_sum_emb = np.zeros((B, self.k), dtype=np.float64)\n        for f in range(F):\n            idx = X[:, f]\n            vf = self.V[idx].astype(np.float64)\n            sum_emb += vf\n            sq_sum_emb += vf ** 2\n\n        # interaction = 0.5 * (||sum_emb||^2 - sum(sq_sum_emb))\n        interaction = 0.5 * (np.sum(sum_emb ** 2, axis=1) - np.sum(sq_sum_emb, axis=1))\n        scores += interaction\n        return scores.astype(np.float32), sum_emb.astype(np.float32), sq_sum_emb.astype(np.float32)\n\n    def _adam_update(self, param, grad, m, v):\n        \"\"\"One Adam step. Returns updated param, m, v.\"\"\"\n        m = self.beta1 * m + (1 - self.beta1) * grad\n        v = self.beta2 * v + (1 - self.beta2) * (grad ** 2)\n        m_hat = m / (1 - self.beta1 ** self.t)\n        v_hat = v / (1 - self.beta2 ** self.t)\n        param = param - self.lr * m_hat / (np.sqrt(v_hat) + self.eps)\n        return param, m, v\n\n    def predict(self, X, bs=200_000):\n        \"\"\"Predict scores for all rows in X.\"\"\"\n        scores = []\n        for start in range(0, len(X), bs):\n            Xb = X[start:start + bs]\n            s, _, _ = self._fm_score(Xb)\n            scores.append(s)\n        return np.concatenate(scores)\n\n    def bpr_step(self, X_pos, X_neg):\n        \"\"\"\n        One BPR update step on a batch of (positive, negative) pairs.\n        X_pos, X_neg: int32 arrays (P, F) where P = number of pairs\n        Returns: mean BPR loss\n        \"\"\"\n        if len(X_pos) == 0:\n            return 0.0\n\n        self.t += 1\n        P, F = X_pos.shape\n\n        # Compute scores for positive and negative items\n        s_pos, sum_pos, sq_pos = self._fm_score(X_pos)\n        s_neg, sum_neg, sq_neg = self._fm_score(X_neg)\n\n        # BPR: loss = -mean(log(sigmoid(s_pos - s_neg)))\n        diff = s_pos.astype(np.float64) - s_neg.astype(np.float64)\n        # sigmoid(diff)\n        sig = 1.0 / (1.0 + np.exp(-np.clip(diff, -30, 30)))\n\n        loss = -np.mean(np.log(sig + 1e-10))\n\n        # Gradient: d_loss/d_diff = -(1 - sigmoid(diff)) per pair\n        # d_loss/d_s_pos = -(1-sig)/P, d_loss/d_s_neg = +(1-sig)/P\n        grad_diff = -(1.0 - sig) / P  # shape (P,)\n\n        # Now backprop through FM for pos and neg\n        # For each row i, d_loss/d_b += grad_diff[i]\n        # d_loss/d_W[idx] += grad_diff[i] * 1  (first-order)\n        # d_loss/d_V[idx,j] += grad_diff[i] * (sum_emb[i,j] - V[idx,j])  (interaction)\n\n        # --- Bias gradient ---\n        db = np.sum(grad_diff) - np.sum(grad_diff)  # cancel: pos and neg each contribute\n        # Actually: grad for s_pos goes to pos rows, grad for s_neg goes to neg rows\n        db_pos = np.sum(grad_diff)\n        db_neg = -np.sum(grad_diff)\n        db = db_pos + db_neg  # = 0, but let's keep it general\n\n        # --- W gradient ---\n        dW = np.zeros(self.n_features, dtype=np.float64)\n        for f in range(F):\n            idx_pos = X_pos[:, f]\n            idx_neg = X_neg[:, f]\n            np.add.at(dW, idx_pos, grad_diff)\n            np.add.at(dW, idx_neg, -grad_diff)\n\n        # L2 regularization on W\n        dW += self.l2 * self.W\n\n        # --- V gradient ---\n        dV = np.zeros((self.n_features, self.k), dtype=np.float64)\n\n        sum_pos_d = sum_pos.astype(np.float64)\n        sum_neg_d = sum_neg.astype(np.float64)\n        V_d = self.V.astype(np.float64)\n\n        for f in range(F):\n            idx_pos = X_pos[:, f]\n            idx_neg = X_neg[:, f]\n\n            # For positive rows: grad = grad_diff[:, None] * (sum_pos - V[idx_pos])\n            v_pos_f = V_d[idx_pos]  # (P, k)\n            g_pos = grad_diff[:, None] * (sum_pos_d - v_pos_f)  # (P, k)\n            np.add.at(dV, idx_pos, g_pos)\n\n            # For negative rows: grad = -grad_diff[:, None] * (sum_neg - V[idx_neg])\n            v_neg_f = V_d[idx_neg]  # (P, k)\n            g_neg = -grad_diff[:, None] * (sum_neg_d - v_neg_f)  # (P, k)\n            np.add.at(dV, idx_neg, g_neg)\n\n        # L2 regularization on V (only for indices that appear)\n        touched = np.unique(np.concatenate([X_pos.ravel(), X_neg.ravel()]))\n        dV[touched] += self.l2 * V_d[touched]\n\n        # --- Adam updates ---\n        self.b, self.m_b, self.v_b = self._adam_update(\n            float(self.b), db, self.m_b, self.v_b\n        )\n        self.b = np.float32(self.b)\n\n        self.W, self.m_W, self.v_W = self._adam_update(\n            self.W.astype(np.float64), dW, self.m_W, self.v_W\n        )\n        self.W = self.W.astype(np.float32)\n\n        self.V, self.m_V, self.v_V = self._adam_update(\n            self.V.astype(np.float64), dV, self.m_V, self.v_V\n        )\n        self.V = self.V.astype(np.float32)\n\n        return float(loss)\n\n\ndef build_bpr_pairs(X, y, users, rng):\n    \"\"\"\n    For each user in batch that has at least one positive and one negative,\n    sample one (positive, negative) pair.\n    \n    Returns: X_pos (P, F), X_neg (P, F) arrays\n    \"\"\"\n    y_arr = np.asarray(y)\n    users_arr = np.asarray(users)\n\n    # Group by user\n    user_to_pos = {}\n    user_to_neg = {}\n\n    for i in range(len(y_arr)):\n        u = users_arr[i]\n        if y_arr[i] == 1:\n            if u not in user_to_pos:\n                user_to_pos[u] = []\n            user_to_pos[u].append(i)\n        else:\n            if u not in user_to_neg:\n                user_to_neg[u] = []\n            user_to_neg[u].append(i)\n\n    pos_indices = []\n    neg_indices = []\n\n    for u in user_to_pos:\n        if u in user_to_neg:\n            # Sample one positive and one negative\n            p_idx = rng.choice(user_to_pos[u])\n            n_idx = rng.choice(user_to_neg[u])\n            pos_indices.append(p_idx)\n            neg_indices.append(n_idx)\n\n    if len(pos_indices) == 0:\n        return np.zeros((0, X.shape[1]), dtype=np.int32), np.zeros((0, X.shape[1]), dtype=np.int32)\n\n    pos_indices = np.array(pos_indices)\n    neg_indices = np.array(neg_indices)\n\n    return X[pos_indices], X[neg_indices]\n\n\n# \u2500\u2500 Hyperparameters \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nBATCH = 8192\nPATIENCE = 4\nMAX_EPOCHS = 50\nN_SEEDS = 5\n\n# \u2500\u2500 Helper: within-user fractional ranks \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ndef within_user_ranks(users, scores):\n    user_arr = np.array(users)\n    ranks = np.zeros(len(scores), dtype=np.float64)\n    unique_users = np.unique(user_arr)\n    for u in unique_users:\n        mask = user_arr == u\n        s = scores[mask]\n        n = mask.sum()\n        if n == 1:\n            ranks[mask] = 0.5\n            continue\n        order = np.argsort(s)\n        r = np.empty(n, dtype=np.float64)\n        r[order] = np.arange(n, dtype=np.float64)\n        r = r / (n - 1)\n        ranks[mask] = r\n    return ranks\n\n\ndef run_bpr_epoch(model, X, y, users, rng, batch_size=BATCH):\n    \"\"\"One full pass over data with BPR pairwise loss.\"\"\"\n    idx = rng.permutation(len(y))\n    X_shuf = X[idx]\n    y_shuf = y[idx]\n    users_shuf = [users[i] for i in idx]\n\n    losses = []\n    n_pairs_total = 0\n\n    for start in range(0, len(y), batch_size):\n        end = min(start + batch_size, len(y))\n        Xb = X_shuf[start:end]\n        yb = y_shuf[start:end]\n        ub = users_shuf[start:end]\n\n        X_pos, X_neg = build_bpr_pairs(Xb, yb, ub, rng)\n        n_pairs = len(X_pos)\n        n_pairs_total += n_pairs\n\n        if n_pairs > 0:\n            loss = model.bpr_step(X_pos, X_neg)\n            losses.append(loss)\n\n    avg_loss = float(np.mean(losses)) if losses else 0.0\n    return avg_loss, n_pairs_total\n\n\ndef train_single_model_bpr(seed, X_tr, y_tr, u_tr, X_va, y_va, u_va, new_dim):\n    \"\"\"Train one FM model with BPR loss, return best model and epoch count.\"\"\"\n    rng = np.random.default_rng(seed)\n    model = FMModel(new_dim, k=16, lr=0.001, l2=1e-6, seed=seed)\n\n    best_val = -1.0\n    best_weights = None\n    patience_left = PATIENCE\n    best_epoch = 0\n\n    for epoch in range(1, MAX_EPOCHS + 1):\n        tr_loss, n_pairs = run_bpr_epoch(model, X_tr, y_tr, u_tr, rng)\n\n        val_scores = model.predict(X_va)\n        val_res = evaluate(u_va, y_va, val_scores)\n        val_primary = val_res['primary']\n\n        print(f\"  Seed {seed} Epoch {epoch:02d}  bpr_loss={tr_loss:.4f}  \"\n              f\"n_pairs={n_pairs}  val_primary={val_primary:.4f}\", flush=True)\n\n        if val_primary > best_val + 1e-6:\n            best_val = val_primary\n            best_weights = (\n                model.V.copy(),\n                model.W.copy(),\n                float(model.b),\n                # Save Adam state too\n                model.m_V.copy(), model.v_V.copy(),\n                model.m_W.copy(), model.v_W.copy(),\n                model.m_b, model.v_b,\n                model.t\n            )\n            patience_left = PATIENCE\n            best_epoch = epoch\n        else:\n            patience_left -= 1\n            if patience_left <= 0:\n                print(f\"  Seed {seed} early stop at epoch {epoch}, best was {best_epoch}\",\n                      flush=True)\n                break\n\n    # Restore best weights\n    if best_weights is not None:\n        model.V[:] = best_weights[0]\n        model.W[:] = best_weights[1]\n        model.b = np.float32(best_weights[2])\n\n    return model, best_epoch\n\n\nif args.train_split == 'train':\n    # \u2500\u2500 Load random data for unbiased evaluation \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    rand_rows = load_random_valid(args.data_dir)\n    X_rand_base, y_rand, u_rand, _ = encode_like_train(splits['train'], rand_rows)\n\n    rand_video_rates = make_bucket_feature(rand_rows, video_ctr_map, video_global_rate, key_idx=2)\n    rand_author_rates = make_bucket_feature(rand_rows, author_ctr_map, author_global_rate, key_idx=3)\n    rand_video_buckets = compute_buckets_from_rates(train_video_rates, rand_video_rates, N_BINS)\n    rand_author_buckets = compute_buckets_from_rates(train_author_rates, rand_author_rates, N_BINS)\n    X_rand = extend_features(X_rand_base, rand_video_buckets, rand_author_buckets)\n\n    # \u2500\u2500 Train N_SEEDS models and collect scores \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    va_rank_sum = np.zeros(len(y_va), dtype=np.float64)\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n    tr_rank_sum = np.zeros(len(y_tr), dtype=np.float64)\n    rand_rank_sum = np.zeros(len(y_rand), dtype=np.float64)\n\n    best_epochs = []\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} (BPR) ===\", flush=True)\n        model, best_epoch = train_single_model_bpr(\n            s, X_tr, y_tr, u_tr, X_va, y_va, u_va, NEW_DIM\n        )\n        best_epochs.append(best_epoch)\n\n        # Compute scores for each split\n        va_scores = model.predict(X_va)\n        te_scores = model.predict(X_te)\n        tr_scores = model.predict(X_tr)\n        rand_scores = model.predict(X_rand)\n\n        # Convert to within-user fractional ranks\n        va_ranks = within_user_ranks(u_va, va_scores)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        tr_ranks = within_user_ranks(u_tr, tr_scores)\n        rand_ranks = within_user_ranks(u_rand, rand_scores)\n\n        va_rank_sum += va_ranks\n        te_rank_sum += te_ranks\n        tr_rank_sum += tr_ranks\n        rand_rank_sum += rand_ranks\n\n    # Average ranks\n    va_final = va_rank_sum / N_SEEDS\n    te_final = te_rank_sum / N_SEEDS\n    tr_final = tr_rank_sum / N_SEEDS\n    rand_final = rand_rank_sum / N_SEEDS\n\n    print(f\"\\nBest epochs across seeds: {best_epochs}\", flush=True)\n\n    # \u2500\u2500 Evaluate \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    tr_res = evaluate(u_tr, y_tr, tr_final)\n    va_res = evaluate(u_va, y_va, va_final)\n\n    # Unbiased evaluation using averaged ranks\n    unbiased = unbiased_primary(\n        args.data_dir, splits['train'],\n        lambda rows: rand_final\n    )\n\n    print(f\"TRAIN_PRIMARY={tr_res['primary']:.4f}\")\n    print(f\"VAL_GAUC={va_res['GAUC']:.4f}\")\n    print(f\"VAL_NDCG5={va_res['nDCG@5']:.4f}\")\n    print(f\"VAL_PRIMARY={va_res['primary']:.4f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.4f}\")\n\n    # \u2500\u2500 Write submissions \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        splits['valid'], va_final\n    )\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )\n\nelse:\n    # train+valid mode\n    X_all = np.concatenate([X_tr, X_va], axis=0)\n    y_all = np.concatenate([y_tr, y_va], axis=0)\n    u_all = list(u_tr) + list(u_va)\n\n    FIXED_EPOCHS = 10\n\n    te_rank_sum = np.zeros(len(y_te), dtype=np.float64)\n\n    for s in range(N_SEEDS):\n        print(f\"\\n=== Training seed {s} (train+valid, BPR) ===\", flush=True)\n        rng = np.random.default_rng(s)\n        model = FMModel(NEW_DIM, k=16, lr=0.001, l2=1e-6, seed=s)\n\n        for epoch in range(1, FIXED_EPOCHS + 1):\n            tr_loss, n_pairs = run_bpr_epoch(model, X_all, y_all, u_all, rng)\n            print(f\"  Seed {s} Epoch {epoch:02d}  bpr_loss={tr_loss:.4f}  n_pairs={n_pairs}\", flush=True)\n\n        te_scores = model.predict(X_te)\n        te_ranks = within_user_ranks(u_te, te_scores)\n        te_rank_sum += te_ranks\n\n    te_final = te_rank_sum / N_SEEDS\n\n    submit.write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        splits['test'], te_final\n    )", "plan": "", "spec": {"hypothesis": "Replacing pointwise BCE with BPR pairwise loss \u2014 sampling within-user (positive, negative) pairs and training on sigmoid(score_pos - score_neg) \u2014 will improve GAUC and thus validation primary by optimizing the same objective the metric measures.", "mechanism": "GAUC is exactly the per-user AUC, which equals the fraction of (pos, neg) pairs where the positive is ranked higher. BPR loss directly maximizes this probability at training time. Pointwise BCE treats each row independently and does not see the within-user contrast that the metric measures. With only ~5 items per user in evaluation, the pairwise ordering matters more than the absolute score calibration.", "evidence": {"task_structure": "GAUC = per-user AUC = fraction of within-user (pos,neg) pairs correctly ordered. BPR is the direct surrogate for this. ~5 impressions/user means the rank order dominates. Existing gap (train 0.6710, valid 0.6034) is large, suggesting BCE is not well-matched to the ranking objective.", "previous_experiments": "Node 4 attempted BPR and crashed with an exception (buggy implementation, not a tested hypothesis). Node 3 is the parent \u2014 we build BPR on top of node 3's features (5 fields + 2 CTR buckets, 5-seed ensemble with rank averaging).", "literature": ""}, "target_metric": "GAUC", "proposed_change": "Replace the pointwise BCE training step (model.step) with a BPR pairwise loss. In each minibatch: (1) find users that have at least one positive and one negative in the batch, (2) for each such user sample one positive and one negative row, (3) compute BPR loss = -mean(log(sigmoid(score_pos - score_neg))). Keep all other aspects of node 3 unchanged: 7 features, 5-seed ensemble, rank averaging.", "expected_result": "Validation primary increases by 0.002-0.004 above node 3 (from 0.6034 to ~0.605-0.607), primarily through GAUC improvement.", "falsification_condition": "If validation primary stays below 0.6054 (within noise of node 3), or if train primary collapses below 0.60 indicating BPR is not learning, the hypothesis is wrong.", "risks": {"leakage": "None \u2014 BPR pairs are formed within-batch from the same training rows, no future information used.", "overfitting": "BPR samples pairs within batch which reduces effective batch size. Could underfit if pairs are too sparse. Mitigated by large batch size (8192) and the fact that train has ~44 rows/user so many pos/neg pairs exist.", "runtime": "BPR batch construction adds O(batch_size) overhead but is still O(N) per epoch. Should be comparable to node 3's runtime (~5 min for 5 seeds)."}, "implementation_scope": ["solution.py"], "tags": ["ranking-objective", "bpr", "pairwise-loss", "fm"], "candidates_considered": ["Fix BPR pairwise loss: node 4 crashed due to a bug, not a bad idea. BPR directly optimizes within-user ordering which is exactly what GAUC measures. Expected +0.002 to +0.003 if the mechanism holds.", "Increase ensemble from 5 to 10 seeds: more diversity reduces variance but 5->10 gives diminishing returns (<0.001) and doubles wall-clock cost.", "Add hour-of-day bucket as a feature: top/bottom hours differ by ~0.058 in LV rate. But hourmin is nearly constant within a user's 5-item session, so it barely varies within-user and contributes almost nothing to GAUC."], "reflection": "Node 5 failed due to LLM connection error (not a research question). Node 4 (BPR) failed with an exception \u2014 the idea was never tested. Current best 0.6034 is only +0.0018 over baseline, below the +0.0024 expected from the established findings. The BPR mechanism remains untested and is directly aligned with the within-user ranking objective.", "feature_provenance": null}, "gate_result": "passed_with_warnings", "review_verdict": "CLEAN", "review_reason": "", "train_primary": 0.644, "val_gauc": 0.6711, "val_ndcg5": 0.5373, "val_primary": 0.6042, "unbiased_val_primary": 0.3672, "seeds_averaged": 3, "seed_scores": [0.6042, 0.6042, 0.6042], "seed_unbiased_scores": [0.3672, 0.3672, 0.3672], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "pr_loss=0.5448  n_pairs=84893  val_primary=0.6004\n  Seed 3 Epoch 07  bpr_loss=0.5438  n_pairs=84583  val_primary=0.6005\n  Seed 3 Epoch 08  bpr_loss=0.5419  n_pairs=84773  val_primary=0.6010\n  Seed 3 Epoch 09  bpr_loss=0.5396  n_pairs=84702  val_primary=0.6012\n  Seed 3 Epoch 10  bpr_loss=0.5366  n_pairs=84986  val_primary=0.6004\n  Seed 3 Epoch 11  bpr_loss=0.5331  n_pairs=84395  val_primary=0.6010\n  Seed 3 Epoch 12  bpr_loss=0.5291  n_pairs=84914  val_primary=0.5992\n  Seed 3 Epoch 13  bpr_loss=0.5274  n_pairs=84815  val_primary=0.5993\n  Seed 3 early stop at epoch 13, best was 9\n\n=== Training seed 4 (BPR) ===\n  Seed 4 Epoch 01  bpr_loss=0.6178  n_pairs=84471  val_primary=0.5958\n  Seed 4 Epoch 02  bpr_loss=0.5607  n_pairs=85043  val_primary=0.5981\n  Seed 4 Epoch 03  bpr_loss=0.5554  n_pairs=84468  val_primary=0.5997\n  Seed 4 Epoch 04  bpr_loss=0.5507  n_pairs=84800  val_primary=0.5992\n  Seed 4 Epoch 05  bpr_loss=0.5485  n_pairs=84716  val_primary=0.6002\n  Seed 4 Epoch 06  bpr_loss=0.5481  n_pairs=84659  val_primary=0.6005\n  Seed 4 Epoch 07  bpr_loss=0.5447  n_pairs=84842  val_primary=0.5991\n  Seed 4 Epoch 08  bpr_loss=0.5428  n_pairs=84769  val_primary=0.5997\n  Seed 4 Epoch 09  bpr_loss=0.5390  n_pairs=84608  val_primary=0.5976\n  Seed 4 Epoch 10  bpr_loss=0.5356  n_pairs=85131  val_primary=0.5981\n  Seed 4 early stop at epoch 10, best was 6\n\nBest epochs across seeds: [10, 6, 6, 9, 6]\nTRAIN_PRIMARY=0.6440\nVAL_GAUC=0.6711\nVAL_NDCG5=0.5373\nVAL_PRIMARY=0.6042\nUNBIASED_PRIMARY=0.3672\n", "stderr_tail": "", "exec_time": 421.8845432919916, "submission_ok": true, "accepted": true, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 48526, "tokens_out": 18758, "wall_seconds": 1483.5308108750032}
{"id": 7, "parent_id": null, "operation": "draft", "hypothesis": "The best result so far (0.6042) uses BPR pairwise loss with an FM model plus Bayesian-smoothed CTR features and seed ensembling. The next untested mechanism is **LightGBM with gradient-boosted trees** as the scoring model instead of FM. GBDT can capture non-linear interactions between features natively without discretizing continuous inputs, and with the right feature set (user history statistics crossed with video attributes) it may handle the within-user ranking task better. The key insight: GBDT accepts continuous features directly (duration_ms, smoothed CTR rates), can capture threshold effects (e.g., \"if duration > X and user typically watches long videos\"), and with LambdaRank/LambdaMART objective can optimize ranking directly. Since we're ranking within users, I'll use LightGBM's `lambdarank` objective with query groups defined by user_id. Features: the 5 standard fields encoded + continuous features (duration_ms, video CTR, author CTR computed from training history). Expected gain: +0.003 to +0.010 over baseline if ranking objective + continuous features help.", "code": "#!/usr/bin/env python3\n\"\"\"\nLightGBM LambdaRank model for within-user video ranking on KuaiRand-Pure.\nUses continuous features (duration_ms, smoothed CTR rates) + categorical features.\n\"\"\"\n\nimport argparse\nimport os\nimport sys\nimport numpy as np\nimport pandas as pd\nfrom collections import defaultdict\n\nimport lightgbm as lgb\n\n# Add kit directory to path\nsys.path.insert(0, os.path.join(os.path.dirname(__file__), 'kit'))\n\nfrom data import load, LABEL\nfrom evaluate import evaluate\nfrom submit import write_submission\nfrom unbiased import load_random_valid, encode_like_train, unbiased_primary\n\n\ndef compute_smoothed_ctr(train_rows, field_idx, global_rate, min_count=10):\n    \"\"\"Compute Bayesian-smoothed CTR for a given field (by index in row tuple).\"\"\"\n    counts = defaultdict(int)\n    positives = defaultdict(int)\n    for row in train_rows:\n        key = row[field_idx]\n        counts[key] += 1\n        positives[key] += row[6]  # long_view label\n    \n    smoothed = {}\n    for key in counts:\n        n = counts[key]\n        p = positives[key]\n        # Bayesian smoothing: (positives + global_rate * min_count) / (n + min_count)\n        smoothed[key] = (p + global_rate * min_count) / (n + min_count)\n    \n    return smoothed, global_rate\n\n\ndef build_features(rows, video_ctr, author_ctr, global_video_ctr, global_author_ctr):\n    \"\"\"Build feature matrix for a set of rows.\"\"\"\n    n = len(rows)\n    features = np.zeros((n, 7), dtype=np.float32)\n    \n    for i, row in enumerate(rows):\n        date, user_id, video_id, author_id, tab, duration_ms, label = row\n        \n        # Feature 0: log duration_ms\n        features[i, 0] = np.log1p(duration_ms) if duration_ms > 0 else 0.0\n        \n        # Feature 1: video smoothed CTR\n        features[i, 1] = video_ctr.get(video_id, global_video_ctr)\n        \n        # Feature 2: author smoothed CTR\n        features[i, 2] = author_ctr.get(author_id, global_author_ctr)\n        \n        # Feature 3: tab encoded as int\n        try:\n            features[i, 3] = int(tab)\n        except (ValueError, TypeError):\n            features[i, 3] = -1\n        \n        # Feature 4: duration bucket (log scale, 10 buckets)\n        if duration_ms > 0:\n            log_dur = np.log1p(duration_ms)\n            features[i, 4] = log_dur\n        \n        # Feature 5: ratio of video CTR to author CTR (relative quality signal)\n        vctr = video_ctr.get(video_id, global_video_ctr)\n        actr = author_ctr.get(author_id, global_author_ctr)\n        if actr > 0:\n            features[i, 5] = vctr / actr\n        else:\n            features[i, 5] = 1.0\n        \n        # Feature 6: date (day of week as proxy for temporal effects)\n        # date is like 20220408, extract day of month\n        try:\n            day = int(str(int(date))[-2:])\n            features[i, 6] = day % 7  # day of week approximation\n        except:\n            features[i, 6] = 0\n    \n    return features\n\n\ndef build_groups(rows, users):\n    \"\"\"Build group sizes for LambdaRank (number of items per query/user).\"\"\"\n    user_counts = defaultdict(int)\n    for user in users:\n        user_counts[user] += 1\n    \n    # Groups in same order as users appear\n    seen_users = []\n    seen_set = set()\n    for user in users:\n        if user not in seen_set:\n            seen_users.append(user)\n            seen_set.add(user)\n    \n    groups = [user_counts[u] for u in seen_users]\n    return groups\n\n\ndef get_user_order(rows):\n    \"\"\"Get the sequence of user_ids in row order.\"\"\"\n    return [row[1] for row in rows]\n\n\ndef sort_by_user(rows):\n    \"\"\"Return rows sorted by user_id (required for LambdaRank groups), plus original indices.\"\"\"\n    indexed = list(enumerate(rows))\n    indexed.sort(key=lambda x: x[1][1])  # sort by user_id\n    orig_indices = [x[0] for x in indexed]\n    sorted_rows = [x[1] for x in indexed]\n    return sorted_rows, orig_indices\n\n\ndef within_user_rank_avg(scores, users):\n    \"\"\"Convert scores to within-user fractional ranks.\"\"\"\n    user_to_indices = defaultdict(list)\n    for i, u in enumerate(users):\n        user_to_indices[u].append(i)\n    \n    ranks = np.zeros(len(scores), dtype=np.float32)\n    for u, indices in user_to_indices.items():\n        s = scores[indices]\n        # Fractional rank: rank 0 = lowest score\n        order = np.argsort(s)\n        n = len(indices)\n        for rank_pos, orig_idx in enumerate(order):\n            ranks[indices[orig_idx]] = rank_pos / max(n - 1, 1)\n    \n    return ranks\n\n\ndef train_lgbm(X_train, y_train, groups_train, X_valid, y_valid, groups_valid,\n               seed=0, n_estimators=500, early_stopping_rounds=20):\n    \"\"\"Train a LightGBM LambdaRank model.\"\"\"\n    train_data = lgb.Dataset(\n        X_train, label=y_train, group=groups_train, free_raw_data=False\n    )\n    valid_data = lgb.Dataset(\n        X_valid, label=y_valid, group=groups_valid, free_raw_data=False,\n        reference=train_data\n    )\n    \n    params = {\n        'objective': 'lambdarank',\n        'metric': 'ndcg',\n        'ndcg_eval_at': [5],\n        'learning_rate': 0.05,\n        'num_leaves': 31,\n        'min_data_in_leaf': 20,\n        'feature_fraction': 0.8,\n        'bagging_fraction': 0.8,\n        'bagging_freq': 5,\n        'lambda_l1': 0.1,\n        'lambda_l2': 0.1,\n        'seed': seed,\n        'verbose': -1,\n        'n_jobs': -1,\n    }\n    \n    callbacks = [\n        lgb.early_stopping(stopping_rounds=early_stopping_rounds, verbose=False),\n        lgb.log_evaluation(period=-1),\n    ]\n    \n    model = lgb.train(\n        params,\n        train_data,\n        num_boost_round=n_estimators,\n        valid_sets=[valid_data],\n        callbacks=callbacks,\n    )\n    \n    return model\n\n\ndef main():\n    parser = argparse.ArgumentParser()\n    parser.add_argument('--data_dir', required=True)\n    parser.add_argument('--out_dir', required=True)\n    parser.add_argument('--seed', type=int, default=0)\n    parser.add_argument('--train_split', default='train', choices=['train', 'train+valid'])\n    args = parser.parse_args()\n    \n    os.makedirs(args.out_dir, exist_ok=True)\n    rng = np.random.RandomState(args.seed)\n    \n    # Load data\n    splits = load(args.data_dir)\n    train_rows = splits['train']\n    valid_rows = splits['valid']\n    test_rows = splits['test']\n    \n    # Compute global label rate\n    global_rate = np.mean([row[6] for row in train_rows])\n    \n    # Compute smoothed CTR from training data only\n    video_ctr, global_video_ctr = compute_smoothed_ctr(train_rows, 2, global_rate, min_count=10)\n    author_ctr, global_author_ctr = compute_smoothed_ctr(train_rows, 3, global_rate, min_count=5)\n    \n    if args.train_split == 'train+valid':\n        # Train on train+valid, score test\n        combined_rows = train_rows + valid_rows\n        \n        # Recompute CTR with train+valid\n        combined_video_ctr, combined_global_video_ctr = compute_smoothed_ctr(\n            combined_rows, 2, global_rate, min_count=10\n        )\n        combined_author_ctr, combined_global_author_ctr = compute_smoothed_ctr(\n            combined_rows, 3, global_rate, min_count=5\n        )\n        \n        # Sort rows by user for LambdaRank\n        sorted_combined, _ = sort_by_user(combined_rows)\n        sorted_test, test_orig_idx = sort_by_user(test_rows)\n        \n        X_combined = build_features(sorted_combined, combined_video_ctr, combined_author_ctr,\n                                     combined_global_video_ctr, combined_global_author_ctr)\n        y_combined = np.array([row[6] for row in sorted_combined], dtype=np.float32)\n        users_combined = [row[1] for row in sorted_combined]\n        groups_combined = build_groups(sorted_combined, users_combined)\n        \n        X_test_sorted = build_features(sorted_test, combined_video_ctr, combined_author_ctr,\n                                        combined_global_video_ctr, combined_global_author_ctr)\n        \n        combined_data = lgb.Dataset(\n            X_combined, label=y_combined, group=groups_combined, free_raw_data=False\n        )\n        \n        params = {\n            'objective': 'lambdarank',\n            'metric': 'ndcg',\n            'ndcg_eval_at': [5],\n            'learning_rate': 0.05,\n            'num_leaves': 31,\n            'min_data_in_leaf': 20,\n            'feature_fraction': 0.8,\n            'bagging_fraction': 0.8,\n            'bagging_freq': 5,\n            'lambda_l1': 0.1,\n            'lambda_l2': 0.1,\n            'seed': args.seed,\n            'verbose': -1,\n            'n_jobs': -1,\n        }\n        \n        # Use fixed number of rounds (use 200 as a reasonable fixed schedule)\n        model = lgb.train(params, combined_data, num_boost_round=200)\n        \n        # Predict on test (sorted order)\n        test_scores_sorted = model.predict(X_test_sorted)\n        \n        # Un-sort test scores back to original order\n        test_scores = np.zeros(len(test_rows), dtype=np.float32)\n        for sorted_idx, orig_idx in enumerate(test_orig_idx):\n            test_scores[orig_idx] = test_scores_sorted[sorted_idx]\n        \n        write_submission(\n            os.path.join(args.out_dir, 'submission_test.csv'),\n            test_rows, test_scores\n        )\n        return\n    \n    # --- Normal training path ---\n    \n    # Sort rows by user for LambdaRank (required: all rows for same user must be contiguous)\n    sorted_train, train_orig_idx = sort_by_user(train_rows)\n    sorted_valid, valid_orig_idx = sort_by_user(valid_rows)\n    \n    # Build features\n    X_train = build_features(sorted_train, video_ctr, author_ctr, global_video_ctr, global_author_ctr)\n    y_train = np.array([row[6] for row in sorted_train], dtype=np.float32)\n    users_train = [row[1] for row in sorted_train]\n    groups_train = build_groups(sorted_train, users_train)\n    \n    X_valid = build_features(sorted_valid, video_ctr, author_ctr, global_video_ctr, global_author_ctr)\n    y_valid = np.array([row[6] for row in sorted_valid], dtype=np.float32)\n    users_valid = [row[1] for row in sorted_valid]\n    groups_valid = build_groups(sorted_valid, users_valid)\n    \n    # Train model\n    model = train_lgbm(\n        X_train, y_train, groups_train,\n        X_valid, y_valid, groups_valid,\n        seed=args.seed, n_estimators=500, early_stopping_rounds=30\n    )\n    \n    print(f\"Best iteration: {model.best_iteration}\", file=sys.stderr)\n    \n    # Predict on train (sorted order, un-sort)\n    train_scores_sorted = model.predict(X_train)\n    train_scores = np.zeros(len(train_rows), dtype=np.float32)\n    for sorted_idx, orig_idx in enumerate(train_orig_idx):\n        train_scores[orig_idx] = train_scores_sorted[sorted_idx]\n    \n    # Predict on valid (sorted order, un-sort)\n    valid_scores_sorted = model.predict(X_valid)\n    valid_scores = np.zeros(len(valid_rows), dtype=np.float32)\n    for sorted_idx, orig_idx in enumerate(valid_orig_idx):\n        valid_scores[orig_idx] = valid_scores_sorted[sorted_idx]\n    \n    # Evaluate\n    train_users = [row[1] for row in train_rows]\n    train_labels = [row[6] for row in train_rows]\n    train_metrics = evaluate(train_users, train_labels, train_scores)\n    \n    valid_users = [row[1] for row in valid_rows]\n    valid_labels = [row[6] for row in valid_rows]\n    valid_metrics = evaluate(valid_users, valid_labels, valid_scores)\n    \n    # Unbiased evaluation\n    def score_random_rows(rand_rows):\n        sorted_rand, rand_orig_idx = sort_by_user(rand_rows)\n        X_rand = build_features(sorted_rand, video_ctr, author_ctr, global_video_ctr, global_author_ctr)\n        rand_scores_sorted = model.predict(X_rand)\n        rand_scores = np.zeros(len(rand_rows), dtype=np.float32)\n        for sorted_idx, orig_idx in enumerate(rand_orig_idx):\n            rand_scores[orig_idx] = rand_scores_sorted[sorted_idx]\n        return rand_scores\n    \n    unbiased = unbiased_primary(args.data_dir, train_rows, score_random_rows)\n    \n    print(f\"TRAIN_PRIMARY={train_metrics['primary']:.6f}\")\n    print(f\"VAL_GAUC={valid_metrics['GAUC']:.6f}\")\n    print(f\"VAL_NDCG5={valid_metrics['nDCG@5']:.6f}\")\n    print(f\"VAL_PRIMARY={valid_metrics['primary']:.6f}\")\n    print(f\"UNBIASED_PRIMARY={unbiased:.6f}\")\n    \n    # Write submissions\n    write_submission(\n        os.path.join(args.out_dir, 'submission_valid.csv'),\n        valid_rows, valid_scores\n    )\n    \n    # Score test\n    sorted_test, test_orig_idx = sort_by_user(test_rows)\n    X_test_sorted = build_features(sorted_test, video_ctr, author_ctr, global_video_ctr, global_author_ctr)\n    test_scores_sorted = model.predict(X_test_sorted)\n    test_scores = np.zeros(len(test_rows), dtype=np.float32)\n    for sorted_idx, orig_idx in enumerate(test_orig_idx):\n        test_scores[orig_idx] = test_scores_sorted[sorted_idx]\n    \n    write_submission(\n        os.path.join(args.out_dir, 'submission_test.csv'),\n        test_rows, test_scores\n    )\n\n\nif __name__ == '__main__':\n    main()", "plan": "", "spec": {"tags": ["gbdt", "lambdarank", "continuous-features", "pairwise-loss"], "proposed_change": "The best result so far (0.6042) uses BPR pairwise loss with an FM model plus Bayesian-smoothed CTR features and seed ensembling. The next untested mechanism is **LightGBM with gradient-boosted trees** as the scoring model instead of FM. GBDT can capture non-linear interactions between features natively without discretizing continuous inputs, and with the right feature set (user history statistics crossed with video attributes) it may handle the within-user ranking task better. The key insight: GBDT accepts continuous features directly (duration_ms, smoothed CTR rates), can capture threshold effects (e.g., \"if duration > X and user typically watches long videos\"), and with LambdaRank/LambdaMART objective can optimize ranking directly. Since we're ranking within users, I'll use LightGBM's `lambdarank` objective with query groups defined by user_id. Features: the 5 standard fields encoded + continuous features (duration_ms, video CTR, author CTR computed from training history). Expected gain: +0.003 to +0.010 over baseline if ranking objective + continuous features help.", "mechanism": "", "hypothesis": "The best result so far (0.6042) uses BPR pairwise loss with an FM model plus Bayesian-smoothed CTR features and seed ensembling. The next untested mechanism is **LightGBM with gradient-boosted trees** as the scoring model instead of FM. GBDT can capture non-linear interactions between features natively without discretizing continuous inputs, and with the right feature set (user history statistics crossed with video attributes) it may handle the within-user ranking task better. The key insight: GBDT accepts continuous features directly (duration_ms, smoothed CTR rates), can capture threshold effects (e.g., \"if duration > X and user typically watches long videos\"), and with LambdaRank/LambdaMART objective can optimize ranking directly. Since we're ranking within users, I'll use LightGBM's `lambdarank` objective with query groups defined by user_id. Features: the 5 standard fields encoded + continuous features (duration_ms, video CTR, author CTR computed from training history). Expected gain: +0.003 to +0.010 over baseline if ranking objective + continuous features help."}, "gate_result": "passed", "review_verdict": "CLEAN", "review_reason": "", "train_primary": 0.631523, "val_gauc": 0.661741, "val_ndcg5": 0.533785, "val_primary": 0.597763, "unbiased_val_primary": 0.363287, "seeds_averaged": 1, "seed_scores": [0.597763], "seed_unbiased_scores": [0.363287], "is_buggy": false, "buggy_reason": "", "exception_type": null, "stdout_tail": "TRAIN_PRIMARY=0.631523\nVAL_GAUC=0.661741\nVAL_NDCG5=0.533785\nVAL_PRIMARY=0.597763\nUNBIASED_PRIMARY=0.363287\n", "stderr_tail": "Best iteration: 74", "exec_time": 12.869314665964339, "submission_ok": true, "accepted": false, "reflection": "", "recovery_action": null, "debug_depth": 0, "tokens_in": 14328, "tokens_out": 5209, "wall_seconds": 68.09012287500082}
