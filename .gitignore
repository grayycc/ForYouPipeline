# Data (downloaded from kuairand.com, not tracked)
KuaiRand-Pure/
data/
*.tar.gz
*.tar
*.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets
.env

# Agent runs. Logs and summaries are deliverables and stay tracked; generated CSVs and
# per-node intermediates do not. The final submission is added explicitly with `git add -f`
# when the graded run finishes.
runs/*/nodes/
runs/*/*.csv
runs/smoke*/

# OS
.DS_Store
